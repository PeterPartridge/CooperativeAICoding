---
form: page-brief
page: "Developer Area"
solution: "CoperativeAI"
depends-on: ["workspaceShell.md", "CoperativeAIdb/TeamMember-model.json"]
status: built            # blank | filled | approved | built
---

# Page Brief — Developer Area

> **Who fills this in:** Product describes what the page is for; Developers add the building details.
>
> **How:** answer each question in plain English directly under its heading. Lines starting with `>` are guidance — anything else you write under a heading is your answer.

---

## Part 1 — What This Page Is For *(Product answers — set once)*

### why-exists — Why does this page exist?
So the team is set up in one place: the Develop tab's Developer Area holds the team members (name + role) that Planning assigns work to. The app has no logins — these are names, not accounts.

---

## Part 2 — What It Should Do *(Product answers — can change each round)*

### actions — What should someone be able to do on this page?
- Someone (who: a developer) can: add a team member with a name and a role (Developer / QA / Product / Designer).
- Someone (who: a developer) can: see the team list.
- Someone (who: a developer) can: remove a team member — their assigned work items simply become unassigned.

### look — What should it look like?
A simple team list card in the Develop environment: one row per member with name and role, an add form above it. Develop tab colour as accent.

### information — What information does this page show or collect?
- Team member names and roles. Nothing else — no contact details, no credentials.

### who-can-use — Who is allowed to use this page?
Anyone using the app — single-user local desktop application, no login.

---

## Part 3 — Building Details *(Developers answer)*

### data-stored — What information needs to be stored, and what does each bit look like?
Team members — see [`CoperativeAIdb/TeamMember-model.json`](../CoperativeAIdb/TeamMember-model.json).

### in-memory — Does anything need to be remembered while the page is open (not saved permanently)?
The add form's unsaved input.

### tests — How will we know it works? What should we test?
- Adding a member with a name and role shows them in the list and survives a restart.
- Duplicate names are rejected.
- Removing a member clears them from any work items they were assigned to, without deleting the items.

### limits — Any known limits or things to watch out for?
No.

### model-and-effort — Which AI model and effort level should this page use by default?
Cheapest model, low effort.

---

## Part 4 — changes-over-time

> Each time you come back to improve the page, add a bullet describing what you want to change. Keep changes small.
- Round 50 (my feedback): Fix the relative path escaping the repository too. *(**`../../shared/serve.py` got neither treatment**: it is relative, so the absolute warning never fired, and it was already relative, so nothing resolved it — and it still is not in the repository. It has its own sentence now, because "kept as a full path" would be wrong about it: this one only works where the repository sits beside whatever it names. **Which paths escape cannot be told from the leading dots**, so it is worked out by resolving against the working copy rather than by looking: `../../shared/x` leaves and `../orders/api/x` climbs out and straight back in, and the second is stored as the short form it means. `api/../serve.py` becomes `serve.py`, lexically, so it is stored the same on every machine rather than only where that folder exists. On the backend, `..` is resolved rather than joined on — otherwise a refusal quotes back `C:\repos\orders\..\..\shared\serve.py`, which is nobody's idea of a helpful message — and climbing above the root of the drive is refused, because popping an empty path would have silently turned `../../..` into the current directory.)*
- Round 49 (my feedback): Fix the typed-path warning too. *(**Whether a path travels is a fact about the path, not a memory of how it arrived.** The warning was set by the picker and by nothing else, so `C:\repos\orders\api\serve.py` typed into the box was stored whole with nothing said — and quietly meant a different file on the next machine. There is one function now, `startFromFor`, and both ways in go through it: relative stays relative with its slashes tidied to match what the picker produces, absolute-and-inside is **made relative** because that is the same answer written portably, and absolute-and-outside is kept whole with the warning. It also shows on arrival rather than only after an edit, so a Solution that already holds an unportable path says so when you open it. The box re-keys on what was actually stored, so typing a full path inside the working copy visibly becomes the short form rather than leaving you looking at something the app did not keep.)*
- Round 48 (my feedback): Add the file picker. *(**A folder for Go, a file for the rest** — Delve is given a package, so a file picker that could not select `cmd/api` would be a picker for the one language it cannot pick for. It opens at the Solution's own folder rather than wherever the last dialog was. **What comes back is stored relative to the working copy**, which was the reason the picker was not built the round before: an absolute path is this machine's answer to a question the Solution asks on every machine, and two people with the repository in different folders would each overwrite the other. The resolution reuses the one the debugger's stopped-file lookup already uses, so Windows' case-insensitivity and its backslashes are handled the way they already were. **Outside the working copy there is no relative form**, so it is kept whole and said to be — "which will not be right on another machine" — rather than stored as something that quietly means a different file elsewhere. Cancelling is an answer and changes nothing, including not clearing what was already there.)*
- Round 47 (my feedback): Add a per-Solution "start from" field. *(**The argument against the convention, beside the thing it argues with.** Working out what to launch is a convention per language — the first `main.py`, the built assembly, the package folder — and a convention is right most of the time and impossible to argue with when it is not. This is the argument, and it sits in the Solution's debug panel because that is where the refusal appears. **It is not a Python field.** All four languages are handed the thing they start, because a field that quietly worked for one of four would be worse than none: Delve gets the `cmd/…` package with the main in it, netcoredbg the `.dll` you name rather than the one the build search guessed at, js-debug the entry, debugpy the file. The placeholder changes per language, since one box means four things and "a path" would be true and useless. **Not the run command.** That is a shell line — `npm run dev` — and this is a path an adapter is pointed at; one field for both would mean typing a shell command somewhere it gets passed to a debugger as a filename. **Checked when a session starts, not when it is saved** — it is set once and then forgotten, so the file it names outlives the memory of naming it, and a rename would otherwise hand three adapters three different confusions instead of one clear refusal that says how to clear it. And it takes effect on the next start rather than pretending to reach a program already running.)*
- Round 46 (my feedback): Add the Python launch shape. *(**The hard part was not the protocol.** Go is handed a folder and Delve builds it; C# is handed a built assembly, which either exists or does not; debugpy runs exactly one `.py`, and a folder of Python says nothing about which file is the program. So the entry point is found by convention — `main.py`, `manage.py`, `app.py`, `__main__.py`, `run.py`, `src/main.py`, `src/app.py`, or a single package with a `__main__.py` — and **refused rather than guessed at** when none of them is there, because a debugger pointed at the wrong file starts, runs something, and stops at none of the breakpoints, which reads as a broken debugger rather than a wrong file. Two of them is a real choice and is named rather than decided by alphabet, and virtual environments are skipped because they are full of other people's `__main__.py`. **The program runs under the interpreter that was proved to have debugpy**, not whatever debugpy resolves for itself — on Windows those are routinely different and the program then fails on imports that are plainly installed. Output comes back over DAP rather than through a terminal this app never hands the adapter. And when the file chosen was not the obvious `main.py`, the session says which one it picked. All four languages launch now.)*
- Round 45 (my feedback): Yes, add the install button that runs it in a terminal. *(**On the half of them that can be installed by a command.** Two of the four adapters are a download and an unzip — a button that typed "Download js-debug-dap from github.com/…" at a shell would report `command not found` and read as a broken app rather than a manual step. So the adapter table now carries the runnable command apart from the prose instruction, and the button appears only where there is one: Go's Delve and Python's debugpy. **Only the language crosses the wire.** A terminal is arbitrary execution, so the command is looked up in the backend's own table rather than sent from the page — the same rule the Claude sign-in already follows. It is typed into a real shell rather than run behind the panel, so what ran is in the scrollback with its output and a failed install can be read, corrected and tried again in place. **It does not claim to know when the command finished** — nothing here can tell when a shell is done, so it says so and puts Look again next to the terminal instead of pretending.)*
- Round 44 (my feedback): Re-read the adapter list after an install. *(**A refusal that outlives the reason for it is worse than no check at all** — it is the app insisting on something that stopped being true. The install runs in a terminal outside this app, so nothing here can be told it finished; three things now notice anyway. **Coming back to the window re-reads it**, gated on the last answer having had a gap, because executing every candidate adapter on each alt-tab would be a lot of work for nothing. **"Look again" sits beside every place the install command is shown** — the picker, the Solution's panel and the Debuggers list — for when focus was not enough. And **the read is shared**: three panels asked for the same list and each probed separately, so an install picked up in one left the others still refusing. One read now, one signal, everybody hears it. Two rules it keeps: a re-read that fails leaves the working answer alone rather than turning a hiccup into a refusal, and the Debuggers panel gave up its private copy — two lists of the same facts, with the one on screen not being the one deciding, is exactly the drift this app keeps removing.)*
- Round 43 (my feedback): Check the adapter is installed before the press. *(**The answer was already known and nobody was asking it.** `debug_adapters` runs every candidate — it does not look for a filename, which is what makes it trustworthy on Windows where the `python` on PATH is usually a Store stub. That read is now shared, and each Solution in the run picker says what Debug will do to it before anything is pressed: "debugs with Go (Delve)", "Go (Delve) not installed — runs in a shell", or "no debugger for its language". **The install command is on screen and selectable**, not only in a tooltip. The per-Solution Debug button is refused where the adapter is missing, with the same command underneath, instead of offering a press that can only fail. Two things worth knowing about the edges: **a list that could not be read is not proof of absence** — it stays "unknown" and Debug still tries, because refusing on a question that never got answered would be worse than the failure this check exists to avoid. And **a Debug press waits for that read**, because otherwise it races it: the first render says nobody knows, the press goes through, and the verdict lands a moment later with nothing left to decide.)*
- Round 42 (my feedback): Yes, make Debug attach the debugger too. *(**Debug is not "run, plus a debugger" — it is one or the other, per Solution.** A debug adapter starts the program itself: Delve launches the binary, js-debug launches node, netcoredbg launches the built assembly. Typing the run command into a shell as well would start a second copy, and two processes fighting over one port look exactly like a broken debugger. So a Debug press launches the adapter for every picked Solution whose language has one, types nothing at those, and **falls back to a plain shell run for the ones that do not — naming them**, because a Solution that quietly got a run when you pressed Debug leaves you hunting for why the breakpoints never hit. Run and Hot reload are unchanged and never attach a debugger. Debug also now reaches a Solution with no run command at all, which the previous version filtered out — that was exactly the Solution Debug is for.)*
- Round 41 (my feedback): In the work section when I click debug this should kick off the application and just show buttons run and hot reload on a top bar in line with the debug button but in the middle. Also add a drop down to change the command. Also can the console be in the same space as the code, but be a detachable window I detach by using click and drag. When a breakpoint hits we should jump back to the screen. Also for the debugger options like stop every time, every hit and print instead of stopping — can these commands all be drop down boxes with multiple choices. *(**Debug now starts the thing it is named after.** It used to open a board where every Solution then needed attaching and running — three presses to reach the state the word already promised. What runs comes from a picker in the middle of the bar: it defaults to the Solution you are on, and it is a multi-select, because the front end you are reading is not the API you need up to read it. Each row carries **its own command dropdown** — what detection found, what that Solution has been given, and "Something else…" — so the override is where the run is instead of on another panel. **Run and Hot reload sit beside it**, and Hot reload is off unless something picked actually has a watcher. **The console moved in with the code**, because output belongs beside the line that produced it, and **it drags out into a real second window** — 90 pixels of travel, so a click on the header is still a click; there is a button too, because a drag is not reachable from a keyboard. The shell survives the trip by being adopted by id; the debugger's output has no replay, so the pulled-out window says it starts from now rather than pretending otherwise. **A breakpoint hitting now brings you back to the code** — it used to render the file underneath the board you were still looking at. And the three breakpoint boxes became **one control per breakpoint**: closed it says "Stops every time" or "prints · conditional · after 7", open it is a tick per behaviour with the box each one needs. All three can be on at once, because the adapters allow it — and the expression stays free text, because `model.value == -3` is not something anybody could put in a dropdown.)*
- Round 40 (my feedback): In Build plan can we remove Solutions affected and move the branch name, unit tests and what needs to change into the What this changes section. Also add the UI pictures into the section when Screen or components are clicked. And in What this changes remove the New-or-existing dropdown and just show text boxes, offering suggestions by scanning the application for known locations of pages, components etc. Can we define in the Develop rules where different things like screens, services etc will live? *(**Three places were asking about one Solution.** A ticklist of affected Solutions, the list of changes, and a block of branch/tests/notes further down — and the ticklist existed only to make the third appear. There is one block per Solution now, holding everything about it: what changes, the sentence, the tests, the branch, the pictures, the approval and the generated schemas. Picking a Solution attaches it, because that is what "affected" means. **The pictures come out when the block is about something somebody looks at**, and the picture-to-screen pairing happens right there — it used to be in a section nowhere near the row naming the screen, which is how the model ended up with a pile of images and a list of names. **The New-or-existing dropdown is gone**: it asked a question the app can answer, and a wrong answer was one mis-click away. A name the Solution already has is a change to it; anything else is new. **Where things live is now a rule** — a folder per kind in the Develop rules, which agents are told and which the build plan reads to suggest what is already there. Each suggestion says where it came from, because a name recorded by the team and a name read off the disk are different kinds of claim. Nothing is scanned for a kind nobody has placed: guessing that screens are "probably in src/pages" would produce confident suggestions for a repository laid out some other way.)*
- Round 39 (my feedback): In Develop Ready or Board and Build plan I need in the solution in the drop down new and this should show types and have all the types allowed and select a language. When selecting a change we select a solution and have a multi select for types UI, logic, models. Then change type and a multi select of screens then what needs to change in development details. On each save this creates and updates .md and .json so no need to have a write button. *(**"UI, logic and models" is not three words.** Asked what those families contain, the answer was that it depends on what is being built — a front end has services and view models, an API has incoming models, outgoing models and the data models behind them, a database has views and stored procedures. So the vocabulary went from three ids to sixteen, grouped into those three families, with each Solution type carrying its own set. The old three are still in the table under their own names, so nothing had to be migrated. **The form is now the sentence you were describing**: pick a Solution — or make one, from inside the dropdown, with every type the backend knows and a language whose generator actually runs — tick which families change, say new or existing, tick or name the things themselves, then write once what has to happen to them. **Every tick is the save.** The details box writes across everything that block put in, and appends what it said to the development details as its own dated set, so the history is kept rather than overwritten. An Add button takes a second Solution, carrying the sentence over. **The write button is gone**: the `.md` and `.json` are rewritten on every save, and when they cannot be — the Product has no folder yet — the panel says so rather than letting somebody believe a file exists.)*
- Round 38 (my feedback): Keep going. *(**The breakpoints you cannot see.** A breakpoint in a file nobody has open is invisible and still stops the program — the strip above the editor only knows about the open file, so a mark left in a file since closed halted a run with nothing on screen to explain it, and no way to clear it short of remembering where it was. Every breakpoint in the Solution is now listed, and **with what makes each behave differently**: its condition, its hit count, and whether it prints instead of stopping. Two rows that stop very differently would otherwise look identical, which is worse than not listing them. **Removing one goes straight into the running program** — a breakpoint taken off a list and only really removed at the next launch would be worse than one that did nothing, because you would believe it. Also says, on the control itself, what running a frame again **cannot** undo: the stack is rewound, but a file that was written or a message that was sent has still happened, and the moment before pressing is the one time that matters.)*
- Round 37 (my feedback): Keep going. *(**Three things the app was saying that were not true**, found by using it. A **breakpoint reported as unset while the program stopped on it** — nothing is bound until a program actually runs, so the first answer is provisional and the UI had no way to hear the correction. My first fix invented a new message for that; running it against the real debugger proved it wrong twice over, because the correction I invented carried the same provisional answer *and* the protocol already has an event for exactly this. The invention is gone. A **log point's message and the debugger clearing its throat looked identical** — the protocol marks output that came from a known line, so the panel now says which line printed what. And **a run that was written into was not marked as such**: changing a value turns the rest of a session into a story about a program that never ran, and "it does not reproduce" half an hour later had no way of knowing why. It is stated for the rest of the session — not a confirmation beforehand, which would be in the way twenty times an hour and clicked through unread. Also: **the Debug build is now what gets debugged.** Picking the most recent build meant that anyone who had built for release since was debugging optimised code, where lines move and variables vanish — which looks exactly like a broken debugger. And 508 leftover folders in the machine's temporary directory became 1.)*
- Round 36 (my feedback): Why can't the app kick off OAuth? *(**It can — the code saying otherwise was stale**, written before this app grew its own terminals. Two things were wrong. **Asking for the version was never a health check**: on this machine it answers "2.1.227 (Claude Code)" while the sign-in check answers "logged in: false", which is why the Test button reported a working provider right up until the first real turn failed. There is a proper check now; it runs no model, so it costs nothing off the plan and can be asked as often as wanted. And **there was no way to fix it from inside the app** — there is now a Sign in button that opens a terminal on the Build board and starts the login there. It is typed into a shell rather than run as the terminal itself, so it can be answered, abandoned and retried in the same panel, and what it printed stays on screen. Being signed in also counts towards being ready now: installed, added and first in the chain still cannot run a single turn without it. The setup panel used to say in as many words that none of this was possible.)*
- Round 35 (my feedback): [validation output pasted]. *(**The app was accusing models it had never reached.** Five checks returned nothing at all, and the report said the model had invented architecture kinds, proposed a forbidden technology, and invented work from an empty brief. Nobody had seen it do any of those — every call failed before reaching a model. Each check honestly recorded "no usable answer", and then the advice was looked up by the check's *name* alone, ignoring why it failed. **That is the failure this platform exists to prevent, committed by the platform.** A verdict about behaviour needs an answer to have been read, so an unanswered check can no longer produce one; declining still counts as answering, because a model that used the escape hatch has shown its judgement. **And the real reason was buried**: the sign-in had expired, the tool said so plainly in its reply, and the app was printing four hundred characters of raw JSON with that sentence somewhere in the middle — usually truncated away.)*
- Round 34 (my feedback): Continue. *(**A value can now be changed in a running program** — the last obviously-missing piece of an ordinary debugger, and the one with real consequences, because the program carries on from there with a value it would never have computed. The assertion is deliberately **not** "the write succeeded", and not "the row now says 5000" — either would pass against a debugger that acknowledged the write and changed nothing. It is that a **later evaluation of a different expression reflects it**: after setting `tax` to 5000, `subtotal + tax` came to **16810**, which the debugger has to work out afresh and can only reach if the program's own memory really changed. There are **two requests here, not one with a fallback**: one names a value by its container and its name, and so cannot reach `order.Items[0].Price` because nothing holds that under that name; the other takes the expression itself. Delve does the first and not the second, so for Go a local can be changed and a watch cannot — a fifth thing the three debuggers disagree about. Nothing is parsed by this app: the new value goes across in the program's own language, quotes and all, for the same reason a breakpoint condition does, and what comes back is what the value **became** rather than what was typed. A write re-reads the whole frame rather than patching one row, because it can change more than the row it was made on and everything else would otherwise be quietly stale. Escape leaves the program exactly as it was, and where the debugger cannot write, no value is editable at all.)*
- Round 33 (my feedback): Do hover-to-evaluate next. *(**Done — hovering a name in the editor now shows what it comes to**, which is where somebody instinctively looks first, and it is one request away from what the watch pane already did. The protocol's `context` field is **not a label**: it says *why* the value is being asked for, and it changes the answer. Verified both ways against Delve, and the assertion is that **the hover answer equalled the watch answer** for the same name in the same frame — a tooltip telling a different story from the pane beside it would be worse than no tooltip. It also turned up a **fourth thing the three debuggers disagree about**: js-debug and Delve answer hovers, netcoredbg does not. It evaluates perfectly well, so the watch pane works for C#, but it has not said it wants one call per pointer movement — so that is refused before it reaches the wire rather than sent and hoped for. One thing had to change for this to be correct: **the editor now follows the frame you select**, not only where the program stopped. Picking a caller and hovering a name there is a different question, and evaluating it in the innermost frame would have quietly answered the wrong one — so selecting a frame moves the highlight to its line too, which is what selecting it should have meant all along. Where there is no debugger no hover is registered at all, because an editor answering every hover with "no debugger" would be worse than one that stays quiet.)*
- Round 32 (my feedback): Continue. *(**Watch expressions — what the variable list cannot answer.** That list shows what happens to have a name in scope; a watch shows what somebody actually wants to know, and `subtotal + tax`, `len(items)` and `order.Customer.Region` are not variables anywhere, so none of them can be read off a scope however well it is displayed. Verified against Delve, and the assertions are **deliberately not variables**: `subtotal + tax` came back `12995`, and `len(items)` — a call into the program's own standard library — came back `2`. A "watch" that merely looked names up would have passed for `subtotal` and failed both. Expressions are worked out **in the selected frame**, by the debugger, inside the running program, so the same expression against a caller is a different question: evaluating it against `main`, where those parameters do not exist, errors — **and then the very next evaluation in the inner frame still worked**, which is the half that matters. So an out-of-scope watch is an ordinary answer rather than a failure: the message goes on that one row, the other watches are untouched, and nothing is raised over the panel. A watch that comes back a struct **opens like any variable**, through the same machinery, because it is the same kind of answer. The expression outlasts the session; the value does not — a number under an expression that was true one step ago is worse than nothing.)*
- Round 31 (my feedback): Do multiple threads next. *(**Done — and the case it exists for is deadlock**, which is why it was the largest gap left. The thread that stops is rarely the one holding the lock, so a debugger that only ever showed the stopped thread could not show you the problem at all: the interesting stack belongs to somebody else. Every thread is now listed, the one the breakpoint actually hit is marked, and picking any of them shows **its** stack and variables. Verified against Delve, deliberately not racily — a Go program starts three goroutines that signal a `WaitGroup` *before* parking on a channel nothing sends to, so by the time the breakpoint is reached all three certainly exist rather than the test hoping the scheduler got round to them. The assertion that matters: a thread that was **not** the one that stopped still had frames of its own. **Stepping now acts on the thread you picked, and this time it really does** — last round's finding was that a step cannot be aimed at a frame because the protocol's step requests carry a thread and nothing else, and that same fact is what makes this legitimate. The list is re-read at every stop rather than kept, because threads come and go and the protocol's notifications about that are advisory; a list carried over would be offering threads that have since ended. No picker appears for a single-threaded program, and the list goes away while the program runs, because it is a snapshot of a moment that has passed.)*
- Round 30 (my feedback): Continue. *(**The debt I had written down turned out not to be fixable, and finding that out was the round.** "Stepping does not follow the selected frame" had been carried for four rounds as something to build. It cannot be built: the protocol's step requests carry a **thread** and nothing else, so a step always acts on the innermost frame however the stack is selected. No version of this app could make it do otherwise. So the fix is to **say so where the mismatch shows** — select a caller and the panel now says stepping will act on `inner`, naming the frame rather than leaving "the innermost one" to be worked out. And then to offer **what the protocol does give per frame**: `restartFrame`, which puts the program back at the start of the call you picked. Verified against js-debug — stopped deep inside `inner`, restarting `outer` put it back at the top of `outer` with `inner` gone off the stack. The assertion is that it went **backwards**, which is the only thing separating this from any other resume. Gated twice, for two different reasons it might not work: the debugger has to support it at all (js-debug does; Delve and netcoredbg do not), and the particular frame has to be restartable, because a runtime frame cannot be even where its neighbours can. Either one false means no button rather than a button that fails. **It does not undo what the call already did** — a file written or a message sent has still happened — which is worth knowing before pressing it.)*
- Round 29 (my feedback): Do hit counts next. *(**Done — and it turned up the first real difference between the three debuggers.** A hit count is the one thing a condition cannot express: "stop the seventh time this line is reached" needs no variable to test against, and works on a line where nothing in scope counts the iterations. Writing the test against Delve failed immediately on the capability check, so I read what all three actually claim: **js-debug does conditions, log points and hit counts; Delve does conditions and log points but not hit counts; netcoredbg does conditions only.** So the hit-count test runs against js-debug, where it stopped on the seventh hit — `i == 6`, because the hit count is one-based and the loop counter is not, and that off-by-one is what the assertion pins down. **netcoredbg then became the proof that the refusal path is real** rather than a branch nothing reaches: asked for a log point and a hit count it cannot do, it refused both with the reason named and **armed nothing** — no stop arrived. That matters because DAP has no failure for an unsupported extra; the field is simply ignored, so a message sent anyway would become an ordinary breakpoint and stop. The **grammar belongs to the adapter** — js-debug takes `7`, Delve takes `== 7` — so it is passed through exactly as typed rather than parsed here and got wrong for one of them. The Debuggers panel now reports all three and says the absences plainly, which is the same mismatch fixed two rounds ago showing up again.)*
- Round 28 (my feedback): Do log points next. *(**Done — the feature that removes a rebuild.** A message on a breakpoint makes the line print and carry on instead of stopping: the `println` you would otherwise add, without the edit, the rebuild or the tidying up afterwards, and `{expr}` inside it is evaluated in the running program. Verified against Delve on a Go loop of three with `round {i}` on the line inside it — three messages, `round 0`/`1`/`2`, and the program ran to the end **without ever stopping**. That absence is the assertion that matters: a log point accidentally sent as a plain breakpoint would stop on the first iteration, and a test that only checked "the message was printed" would not catch it, because the message arrives either way. **A dropped log message is worse than a dropped condition** — DAP has no failure for either, and an adapter that lacks the capability just ignores the field, so a dropped condition stops *every* time while a dropped message **stops**, at a breakpoint whose whole purpose was not to. Both are refused the same way, with the reason said. A log point also gets **its own mark in the gutter**, hollow rather than the solid red dot, because a mark that looks like a breakpoint and never stops reads as a debugger that is broken. Breakpoints stored before this still load, including ones written between the last two shapes.)*
- Round 27 (my feedback): Do conditional breakpoints next. *(**Done, and it closes a mismatch the app was already showing**: the Debuggers panel reported that every adapter supports conditional breakpoints and there was nowhere to set one. Each breakpoint in the open file now has a condition box. Verified against Delve on a Go loop of ten with `i == 7` — it stopped **once**, at `i == 7`, with `total == 21` (0+1+…+6), so the six earlier iterations really did run through. That is the assertion that matters: a dropped condition would stop at `i == 0`, and a test that only checked "it stopped somewhere" would pass either way. **A condition an adapter cannot evaluate holds its breakpoint back rather than arming it** — DAP has no failure there, an adapter without the capability just ignores the field and stops *every* time, which is the opposite of what was asked for. So the capability is read from the handshake, the breakpoint is not sent, and the refusal says why and what to do about it. A refusal now carries the adapter's own words rather than a bare count. The condition box also says **whose language** the expression is in, because the adapter evaluates it inside the running program — a Go Solution takes a Go expression, not JavaScript. Breakpoints stored before this still load: the old shape was a bare list of line numbers and is converted on read rather than dropped.)*
- Round 26 (my feedback): Continue. *(**Variables open now.** A stop used to show `order` as `main.Order {...}` with the words "has fields" beside it — a label where a control belonged. Clicking it opens it, verified against Delve with a real struct: `Subtotal = 11810` and `Tax = 1185` read out of the live process. Levels are fetched **one at a time, on opening**, because a `variablesReference` is a handle to something the adapter has not sent — walking them eagerly would read the whole object graph on every stop, follow a linked list to its end, and never finish on a cyclic one. **Every handle dies when the program moves**, so what was open is forgotten on a step, on the program ending, and on picking a different frame, and reopening re-fetches rather than showing a remembered value that is no longer true. A failed opening says why on the row it failed on, because drawing an empty struct instead would read as "it has no fields" — a different and wrong claim. A scalar gets no caret at all rather than one that would fail.)*
- Round 25 (my feedback): Continue. *(**C# debugs for real now** — three of four languages. Verified the same way as Go and TypeScript: a scratch console app, a breakpoint on `int total = subtotal + tax;`, and an assertion that it stopped there with `subtotal = 11810` and `tax = 1185` read out of the live process. First, though, the **stdio hang** round 24 flagged had to go: a pipe has no read timeout, so the poll added for js-debug covered TCP only and an adapter that went quiet blocked the caller forever. The reader moved to its own thread handing chunks over a channel, so the wait can actually time out — one shape for both transports. C# is **netcoredbg, not vsdbg**: Microsoft's is the better debugger but its licence permits use only from Visual Studio and VS Code, so driving it from here would be a breach; Samsung's is MIT and speaks the same protocol. C# is also the first language whose launch target is **not source** — netcoredbg debugs a built `.dll`, and finding the right one is fiddlier than it looks, because `obj/` holds intermediate assemblies, `ref/` holds reference assemblies containing no code, and the output folder holds every dependency. The marker used is a sibling `.runtimeconfig.json`, which the SDK writes only for something meant to be executed. **Nothing built is said plainly** — "run `dotnet build` there first" — rather than starting a debugger against a file that is not there. The install instruction the app showed was also wrong before anyone used it, the same way `@vscode/js-debug` was, and now names the release zip. **Python is the last one and cannot be verified here**: there is no real Python on this machine, only the Store alias stub that prints "Python was not found", so building a launch shape for it would be guesswork.)*
- Round 24 (my feedback): Commit, and continue. *(Committed rounds 22–23 on a branch, then built the piece round 23 documented: **TypeScript debugging works**, verified the same way Go was — a real Node program stopped on a real line with `subtotal = 11810` read out of the live process. js-debug is a *server*: it answers `launch` with a **`startDebugging` reverse request**, and the client must reply and open a **second connection**, because the child session is where breakpoints actually hit. So a session is now built around a **`Channel`** — one DAP connection with its own sequence, its own outstanding requests and its own `initialized`. Delve gets one and never grows a second, since it never sends a reverse request; js-debug gets two, and every request about the running program routes to the child. Three things that are easy to get wrong and are now right: **every** reverse request is answered, not just that one, because an unanswered one leaves js-debug waiting forever; `supportsStartDebuggingRequest` has to be declared up front or the child is never offered and the program runs unwatched; and the breakpoints are kept on the session so the child's own handshake can re-send them, the root's copy being only provisional. **Two of four languages debug for real now** — Go and TypeScript/JavaScript. Python and C# are found and speak DAP but still have no launch shape, and neither is installed here to verify against.)*
- Round 23 (my feedback): Continue — **js-debug**, installing it so it could be verified. *(Installing it found the first bug: **`@vscode/js-debug` is not an npm package**, so the command the app told people to run returned a 404. js-debug ships as a GitHub release tarball and inside the VS Code extension, and discovery now looks in both — including scanning the versioned `ms-vscode.js-debug-<version>` extension folders. Then three real bugs, found only by talking to it: **js-debug binds `::1` and nothing else**, so connecting to `127.0.0.1` was refused and read as "the adapter failed to start" — both loopback addresses are tried now; **a blocking read could never time out**, because the deadline was only checked between reads, so an adapter that simply went quiet hung the client forever — the socket has a poll timeout now; and **js-debug never answers `disconnect`**, so waiting for that reply hung shutdown — it is sent and the child killed, which is what actually ends it. The last two also protected the Go path, which was one silent adapter away from the same hang. js-debug's handshake is **verified end to end**. Its **launch is still not wired**, and now for a documented reason rather than a vague one: js-debug is a *server* that answers `launch` with a **`startDebugging` reverse request**, and the client must reply and open a **second connection** — the child session is where breakpoints actually hit. That sequence is captured from a real run and written into `live.rs`; wiring it is the next round.)*
- Round 22 (my feedback): **Highlight the stopped line in the editor.** *(Applied: a stop now opens the file it happened in and draws a band across the line with an arrow in the margin — amber, like every other "stopped" in the app, because green would read as "fine" and this is the state you are meant to act on. The **stepping controls moved above the pane** into their own toolbar: opening the file replaces the Debug board with the editor, and a debugger whose Continue button vanishes the moment it stops is no use. The adapter reports an absolute path and the editor works in repository-relative ones, so `relativeTo` converts back — normalising separators and folding case **only on Windows**, since folding a POSIX path would match two genuinely different files. A frame the working copy does not contain — the Go runtime, a dependency — resolves to **nothing**, deliberately: the toolbar still appears and the stack still lists the frame, but no file is opened and nothing is highlighted, because opening the wrong file would be worse than opening none. The highlight is cleared **before** a step is requested rather than after it answers: the program is already moving by then, and an arrow left on the old line points somewhere it no longer is.)*
- Round 21 (my feedback): Build the **stepping UI**. *(Applied: a debug session sits inside the process card for the Solution it debugs, because running something and stopping it mid-line are two questions about the same repository. **Everything hangs off the `stopped` event** — it names a thread, the thread gives a stack, the innermost frame gives the variables in scope. Nothing is polled: an adapter says when it has stopped and nothing in between, so asking repeatedly would only be a way to be wrong between answers. Continue, step over, step into and step out, offered **only while stopped** — a step button on a running program has nothing to step. **Sync breakpoints** pushes the gutter's current set into the live session, because a breakpoint set mid-run that only took effect next time would be worse than one that did nothing, since you would believe it. A frame with no source is still listed as a frame — hiding the runtime internals would make the stack lie about how the program got there. Which language a Solution debugs as is guessed from what it was created as and **named on screen**, and only Go's launch is built, so the button is disabled for the others with the reason beside it rather than failing on the press.)*
- Round 20 (my feedback): Start on breakpoints and stepping. *(**Breakpoints work, proved against a real Go program.** A live session (`debug/live.rs`) with one reader thread, request/response correlation by `seq`, and the adapter's unsolicited events — `stopped`, `output`, `terminated` — reaching the UI as they happen; the handshake shape from round 19 could not carry a running session, because the interesting half is never a reply to anything. The **launch sequence is the protocol's**: `initialize`, then `launch` sent-but-not-awaited, then on the `initialized` event `setBreakpoints` and `configurationDone` — breakpoints set before `initialized` are dropped silently by most adapters, which is the classic way a debugger appears to work and never stops. The integration test writes a small Go program, breaks on a line in the middle of it, and asserts it **stopped there with `subtotal` and `tax` really set in the live process**. `setBreakpoints`, `stackTrace`, `scopes`/`variables`, continue and all three steps are wired through Tauri commands. In the editor a **click in the gutter sets a breakpoint**, kept per machine like the map's layout, and sent as an absolute path because an adapter matches on what the compiler recorded. **Go launches today**; the other three adapters are found and speak DAP but their launch shapes are still to do, and the app says so rather than starting something that never stops.)*
- Round 19 (my feedback): Start **DAP with js-debug first** — and also **C#, Go and Python**. *(The foundation is built and proved: the **wire protocol** (`Content-Length` framing, counted in bytes — a path with an accent in it desynchronises a character-counting decoder permanently), a **session** that starts an adapter over either transport and completes the `initialize` handshake, and **discovery for all four languages**. Adapters: **js-debug** for TypeScript/JavaScript, **debugpy** for Python, **Delve** for Go, **netcoredbg** for C# — netcoredbg rather than Microsoft's vsdbg, whose licence permits use only from Visual Studio and VS Code. Every candidate is **executed, not just found**: on this machine the `python` on PATH is the Microsoft Store stub, a real file that prints an advert and exits, and PATH would have called it available. **Verified end to end against Delve 1.25.2**, which is genuinely installed here — it starts, speaks DAP and reports `configurationDone`. Debug now shows the four languages, what is missing, the one command that installs each, and a **Check** that starts the adapter and completes a real handshake rather than inferring from a filename. **Breakpoints and stepping are not built** — the gutter stays unclickable and the panel says why, because a breakpoint that silently does nothing costs more than none. That is the next round, and it is now request-plumbing down a pipe that works rather than new ground.)*
- Round 18 (my feedback): Build the **Rust process registry**. **Merge "open Solution" and Files** — no Solution picked shows the whole Product, picking one scopes to it, and **any click on a file opens the editor**. Show **installed models** in Admin. And **set breakpoints** — you chose a real **debug adapter (DAP)** over `debugger;` statements. *(Three of the four applied; **DAP is not built and is the next round**, as flagged when the choice was offered — it is per-language adapter work in Rust and does not fit beside the other three. **The registry**: the shells always lived in Rust state, but nothing could ask what was in it, so a panel that unmounted killed its own shell. `list_terminals` and `attach_terminal` are the way back, with a **bounded in-memory replay** so a reattached dev server shows what it has been saying rather than an empty box that reads as a failed start. Debug's shells now survive leaving Build entirely. **The merge**: one Files pane. With no Solution picked it shows every Solution as a foldable root; the Solution bar scopes it; clicking any file opens it in an editor pane. "All Solutions" is a real choice on the bar rather than the absence of one. **Admin** counts the models three ways — installed and usable, awaiting install, failed validation — with installed first and a filter; it still lists everything by default, because defaulting to the filter would hide the Install buttons.)*
- Round 17f (my feedback): The **padding around the dropdowns is too big** — fix it, and **clear the technical debt**. *(The dropdowns were a real bug, and an app-wide one: `min-height: 5.5rem` sat on the shared `input, select, textarea` rule, so **every select and every single-line field in the app was 77px tall**. A minimum writing area is a fact about writing prose, not about form controls — it is `textarea` only now, and selects are 31px and inputs 29px across all four areas. Colour wells got a size of their own too. **Debt cleared:** Debug keeps its shells alive behind other Build panes instead of killing them, gained **Restart** (Ctrl-C then the command, both typed into the shell) and a **likely port** — labelled a guess, from the same table the preview uses, which moved to `lib/devServer.ts` so the two cannot drift; the uptime clock stopped re-rendering the whole board once a second; Solutions with no working copy can be hidden. The **map** gained ⌘/Ctrl+wheel zoom about the pointer, remembers its zoom and pan, and its surface grows with the content instead of stopping at a fixed 3000×2000. **Rules** windows "where agents stopped" to 7 days with the older ones counted rather than dropped, takes which field leads from the field list rather than a hard-coded name, and draws the strategy fields as cards like the rules beside them. **Work**'s busy rows link into Build instead of naming a state and going nowhere, and the readiness rule is now unit-tested directly. The Solution palette went from five hues to eight. What is **not** fixed is everything needing Rust: a process registry so shells survive leaving Build, `list_product_plans` so Ready can score the test plan, a per-run test filter, and a reason code on a blocked job.)*
- Round 17e (my feedback): There is **no debug feature** — build one. Also **reduce the padding** around the Product and Develop headers, and **move Product up beside the tabs**. *(All three applied. **Debug** is a green ▶ chip in the Solution bar opening a board that runs **every Solution at once** — a real PTY per Solution in its own working copy, with that Solution's detected run command ready to type in, several up side by side. That is the thing the old single terminal could never do. What it still does **not** draw is the debugger the design showed — step in/over/out, a call stack, editable variables, breakpoints, per-run environment overrides — because there is no debug adapter behind any of it, and a breakpoint that silently did nothing would cost more than no breakpoint. Nor CPU or memory: the app does not read the process table. It shows what it genuinely knows — which shells it opened, where, what is running in them, and for how long. Nothing is attached until asked, because a shell is a real child process. **Padding**: the page frame went from 1.25/1.5rem to 0.6/0.85rem and the heading tightened. **Product** moved onto the tab row, right-aligned — it was costing a whole row to a control that changes about once a session.)*
- Round 17d (my feedback): Do **Rules** as a **restyle**. *(Applied, and restyle is the right word — the Technical Strategy and Developer Rules editors keep their fields, their saves and their aria-labels; what changed is how they read. **Architecture requirements** now leads as the standing direction rather than being the second of six equal boxes, and each of the seven rules is a card with a level badge. The design had three levels (Blocking / Warn / Advisory) and a count beside every rule — "212 checks · 4 blocks this week". The app has **two** levels and **no** counts: `disallowedTech` is stated as a prohibition *and* read back against the answer, the other six are stated and believed, and nothing counts how often any of them fired. Labelling all seven Blocking would have been the flattering lie. The design's opening scoreboard — 68% of agent PRs merged first try, an 11-minute median — is not there for the same reason: the app watches no pull requests and times no tickets. Its sidebar, **"Where agents stopped"**, is real: blocked and failed jobs from the queue, each with the reason the agent gave in its own words. That completes all four views.)*
- Round 17c (my feedback): Do **Map** next. *(Applied: the architecture map is the design's **three panes** — what can go on the map and what is wrong with it down the left, the canvas in the middle, the selected box down the right — with **zoom, pan, 10px snap and a grid** added, and Arrange/Connect/Save/the .drawio export unchanged behind them. Two things in the design were **not** built because the app cannot see them: the red/amber/green **health light** on each module, and the **owner / size / coverage** stats in the inspector. In their place the app shows the three things it does know about a Solution — whether there is a working copy on this machine, whether a repository is linked, and what it was started as — and the **Map checks** panel names the Solutions failing each. Boxes are still real Solutions: adding one creates it, because a module that exists only in the picture would be a lie about the code. The inspector links the agent inside a Solution through to **Build**, which closes the loop the other way from 17b's lane card.)*
- Round 17b (my feedback): Do **Work** next, and make the Build lane's card **link to the item** rather than start a run. *(Applied. Work opens on a new **Ready** view — the design's work-item list — scoring each item **out of five** things an agent needs: what is asked for, a Solution, how to build it, nothing blocking, and the plan approved. Board, Sprint and List are untouched behind it. **No "ready %"**: the design put a percentage beside "the agent lands it first try", which is a claim about the future; a count of five real checks says the useful part and only the true part. The briefing panel beside it is what the agent would actually be handed, and it **names what is missing**, not just the score. The whole list costs **three Product-wide reads**, not two per item — `Run.planApproved` and `listOpenQuestions` already answer four of the five checks for every item at once. The lane card now opens the first item with no agent in Work: starting a run stays two deliberate presses on the build plan, and a second way to begin one would have routed around the approval gate.)*
- Round 17 (my feedback): Implement the **Develop Workspace** design (Claude Design project *Rust app iOS vscode redesign*): Develop's tabs become **Rules / Work / Build / Map**, and Build becomes four panes — **agents down the left**, the **files they touched** beside them, **one agent's work** in the middle, and **review and ship** on the right. *(Applied for Build; Rules, Work and Map keep their current styling this round, as agreed. Tests and Git stopped being tabs: per agent they are panes of the workbench, Product-wide they moved under Build's "queue, questions and runs" entry. **Three things in the design were deliberately not built, because the app cannot see them**: the per-agent **progress percentage** — the agent's stage is shown instead, which the database really holds; the **token/context figure** in the header, same rule as Claude Code spend; and the **debugger** — call stack, variables, breakpoints and env overrides — which this app has none of, so that tab is the real dev-server and terminal instead. The ship checklist is **derived from the run, not ticked by hand**: four hand-clicked checkboxes would have made the ring a record of what somebody claimed rather than what happened.)*
- Round 16c (my feedback): **Merge Infrastructure and Architecture into one section** — they do the same thing — **with a choice of draw.io or Mermaid**. *(Applied, and it was already half true: `infrastructure` was an architecture-document kind, so the two sections were one feature filed twice. **draw.io is now a format** beside Mermaid, PlantUML and JSON graph, judged by the same validator. One draft works the boxes out from the Solutions and applies the notation at the end, so the two halves cannot disagree about what the architecture is. The only difference on save: Mermaid is text and renders inline; draw.io is text **and** a file, so the real editor can open it. The old Infrastructure component is deleted rather than left as a file imported for one export.)*
- Round 16b (my feedback): **Generate the diagram from the Solutions connected to the Product**, and add a **preview section that updates on add or save**. Solution creation should **start from the "What language will it use?" answer**, and for "other" you add **the command and the language name**. *(Applied: the draft comes from the Solutions, their types and the recorded links — a first draft to correct, since the app cannot know about queues, load balancers or anything third-party. The preview is drawn from the **same grid constants the `.drawio` file uses**, mirrored in both languages with a test on each side asserting identical coordinates, so the picture cannot drift from the file. The language question **is** the starter dropdown rather than a second box asking the same thing, and "something else" requires both a name and a command — recorded as "Elixir" rather than "custom".)*
- Round 16 (my feedback): Rename Develop's **Planning** tab to **Strategy and Rules** and **Workspace** to **Planning and Architecture**, moving Developer Planning into the latter. Planning should show **draw.io for infrastructure** and create draw.io documents. In **Code**, when nothing is open, show the file explorer with a **dropdown of Solutions** to pick. Add a GitHub **commit strategy** — auto-commit on save or every 5 minutes — and **SSH setup**. GitHub should show a **visual history of the branches**. Auto-commits just list the files changed. Manual commits are handled in the code space under a **GitHub panel tabbed with the file explorer**. *(Decisions taken: draw.io **writes real `.drawio` files you open in your own draw.io** — embedding the web editor would break offline and send your infrastructure to a third party; and auto-commit offers **commit locally or commit and push as two separate answers**, your correction to my single three-way choice. All applied. The automatic commit message is only the file list, refuses during a merge, and the SSH private key never reaches the app — you add the public half to GitHub yourself.)*
- Round 15 (my feedback): **Product adds the screens they want on a work item.** Developers then update the work item with the **Solutions to change** and the **screens connected to each Solution**, plus **new APIs or APIs to change**, and **database tables to add** — all depending on the **type of Solution**. Also: when creating a Solution, a **dropdown of languages**, and on create we **run that language's command to create a startup project**. *(Applied: one table holds all of it, because Product's ask and the developer's plan are the same row at different stages — Product adds a screen with no Solution against it, and a developer points it at one. What a Solution can carry comes from its type in one place the UI and the AI prompt both ask, so they cannot disagree: a website has screens, an API has endpoints and the tables behind them, a database has tables only. Starter projects run the toolchain's own generator — every command is shown and editable before Create, runs only in an empty folder, and reports the toolchain's own words when it fails.)*
- Round 14 (my feedback): The Develop **Code** section needs a **terminal** at the bottom, an **AI window** on the left to select Ollama or Claude Code, and a **properties** section at the bottom of the file explorer. Add a **unit test explorer** and a **git explorer hub**, so unit tests across all Solutions can be reviewed and run **regardless of language**, with results. In the Code section add a **git toggle** showing only changed files across all Solutions and what changed. For **merge conflicts**, a window divided into three: my changes, their changes, and the code after the merge. *(Decisions taken: **inspectors first**, and the terminal is to be a **real PTY shell** — portable-pty and xterm.js — so that selecting Claude Code can actually drive its interactive TUI rather than merely hand off to it. **Applied in 14a:** the test explorer, the git hub, the changed-files toggle with per-file diff, and the three-pane merge view. **Applied in 14b:** the PTY terminal at the bottom of Code, the AI window on the left selecting Ollama or Claude Code, and the properties section under the file explorer. All seven asks are now built.)*
- Round 13 (my feedback): **Wire up vision so the AI can see the mockups.** *(Applied: pictures on a build plan are read from disk, encoded, and sent with the generation request — as typed image blocks to Claude, as bare base64 to Ollama. They sit **inside the cached prefix** on the Claude side, because mockups do not change between regenerations of the same work item and are the dearest part of the request. Whether a model can see is **recorded by a person** in AI Settings and is off until they say so: the platform cannot establish it without spending a call, and being wrong costs money either way. The prompt follows what was actually sent — "read the layout from these" when they went, the old "you cannot see them" when they did not. Pictures too large, too many, or of an unsupported type are refused **before** the call, and every omission is named back on the run.)*
- Round 12 (my feedback): **Open a work item** in Develop and plan how it will be built: select the **Solutions affected**, and for each write the **changes required**, **unit tests**, the **branch name** and **what branch to clone from** — with defaults set in the **Develop Strategy**. Add **questions for Product** and **pictures for UI**. Then a button for the **AI to generate the code changes**: once Product and the developers have asked enough, generate the **page and API schemas** to translate into a code solution. *(Applied: a per-(work item, Solution) build plan; questions reuse the AI-feedback channel so answers become clarifications that reach the generation prompt without re-typing; generation produces API/page schemas and expected files per Solution, which the handover brief then carries to whoever writes the code. Schemas rather than raw code, because this app prepares and reviews while the agent writes. ~~**The AI cannot see the pictures**~~ — closed in round 13; a model recorded as able to see is now sent them, and one that is not is still told they exist and asked to say so rather than invent a layout.)*
- Round 11 (my feedback): The file explorer needs a **tab to select multiple Solutions attached to a Product**, so several can be open at once. *(Applied: a tab per open Solution with a picker for the others attached to the selected Product. Each keeps its own tree, folds, open files and unsaved edits, so switching away and back finds it as it was left. The editor is keyed by Solution **and** path, since two Solutions can each hold a `src/main.rs`.)*
- Round 10 (my feedback): Add a **Code** tab showing a code editor in the middle with a **file explorer to the left**, opened by clicking **Open** on a Solution in the Workspace tab. *(Applied: a new CodeEditor component with the explorer left and the editor centre; the Workspace tab's Open button hands the Solution to it. The tree and editor were **moved out of** SolutionBox rather than duplicated — SolutionBox keeps the working-copy path, Open, and the change review, so there is one editor in one place.)*
- Round 9 (my feedback): Separate the Develop area's sections into **tabs**. *(Applied: the ten stacked sections became four tabs grouped by what a developer is doing — **Planning** (technical strategy, developer rules, cross-repo map, architecture), **Work** (board/sprint/list views), **Workspace** (solutions, editor, coding pal, change review, framework files), **Settings** (GitHub, model installs, AI settings). The Product picker stays above the tabs. Plain buttons, not `role="tab"`, because the Work section already owns a Board/Sprint/List tablist.)*
- Round 8c (my feedback): Build the pieces round 8b deferred, as decided: the **full code editor and AI coding pal** (not the minimal option), and **accept always available** on change review — never gated on findings. *(Applied: Monaco editor with save into the working copy; the pal explains/refactors/documents/drafts tests through the same policy, budget and ledger gates as everything else and never writes files itself; Keep/Discard is offered whatever the review found, with the findings recorded on the run first so accepting over a violation is on the record.)*
- Round 8b (my feedback): Rebuild the **Developer Workspace**: open a Solution (file tree, architecture, active components), a real **code editor**, **Claude Code orchestration** with a **change review** of what comes back — checked against the Developer Rules, with per-file accept or reject and the spend for that run — an in-editor **AI coding pal**, and a **cross-repo view**. *(Built this round: opening a Solution, and the change review. Deliberately not built: the editor, which rebuilds part of a tool you already have and is not where this platform's value is; and orchestration, because Claude Code is billed outside this app's ledger so "the spend for that run" is a number the app cannot see. Both are written up in [`../claude-only/CoperativeAI/developerWorkspace.md`](../claude-only/CoperativeAI/developerWorkspace.md).)*
- Round 8 (my feedback): Develop gains a **Developer Planning** sub-area holding Tech Strategy, the Developer Rules (**read-only here — they are edited in Admin**), and Solution Planning: **architecture documents** (system interaction, component map, API contract, event flow, infrastructure), **API contracts** per Solution, and **cross-repo links** between Solutions so a change can be traced to everything it reaches. Every generated diagram must be **validated as parseable before it is stored**.
- Round 2 (my feedback): Move team-member management to the new Admin area (users are assigned roles there). The Develop area keeps solution creation and AI settings, and will gain a Technical Strategy section and Board/Sprint/List views in a later round.
- Round 3 (my feedback): The Develop area needs a **Technical Strategy** section — required infrastructure, architecture requirements, solution creation guidelines, and dependencies / environment prerequisites. It must also offer **Board**, **Sprint**, and **List** views of the work, with **filtering by assigned user**. Pick a Product at the top of the Develop area to work against it.
- Round 6 (my feedback): For every scoped work item the AI must give **two recommendations** — a **fastest** option (most capable model, higher cost, shortest delivery) and a **most cost-efficient** option (cheaper model, longer) — each showing estimated token usage, estimated cost and estimated completion time, and respecting the Product AI budget, token spend limits, the developer rules and the handover logic.
- Round 5 (my feedback): Developers define **Developer Rules** — coding standards, architecture principles, maintainability rules, preferred frameworks, allowed/disallowed technologies, and constraints on AI behaviour — and the AI must follow them when generating code, architecture, plans or recommendations. For each work item the AI builds an **AI-Driven Development Strategy**: a solution strategy describing how to build it, **architecture options** (Windows Service, Azure Web App, Azure Function, API, background worker, or other), and the tech stack that follows from the rules.
- Round 4 (my feedback): The Develop area needs a **GitHub connection** so a Solution can be linked to a GitHub repository, or a new repository created from the app as **private or public**. See [`solutionCreation.md`](solutionCreation.md) round 2 for the Solution side.
- Round 51 (my feedback): Remove "Where things live" from the Develop area's Developer Rules. *(Three reasons, all mine: a developer sets where things live when they create solutions; solutions are never a set plan, because a successful product adds more of them and reshapes the architecture for load; and Product people do not set software architecture or solutions — Product owns UI/UX and stories, Develop owns the nuts and bolts. So it moved onto the Solution, editable at any time, in Develop → Solutions.)*
- Round 52 (my feedback): Condense the build plan and remove the duplicate fields. *(The real duplicate was "Lands in": it and the Solution attachment in "What this changes" were two independent fields answering the same question, and they could disagree — the handover gate and AI-written tests read one, the runs and the plan read the other. Attaching is now the only way to say it.)*
- Round 53 (my feedback): Add the MCP servers to use into the Developer Rules. *(The deferred 2026-08-15 item, now scoped as a rule rather than configuration: the list travels with the constraints an agent is given, and blank is stated as "none named" so silence cannot read as permission.)*
- Round 53 (my feedback): Add the MCP servers to use into the Developer Rules.
- Round 54 (my feedback): Remove "Development details — how this should be built" — it is covered by the Developer Rules and by "what needs to change" per Solution.
- Round 55 (my feedback): Get rid of the "Prepare for an agent" step — save should prepare. *(It could not just be called on save: preparing created a new run row and brief file each time, so `change_run::prepare` is idempotent while a run is unstarted.)*
- Round 56 (my feedback): Show what Product has created and wants in its own read-only tab, with the questions for Product in it so it reads like a chat. *(Read-only because Product owns that side — a requirement reworded by the person implementing it stops being a requirement. Questions stayed per work item; anchoring them to a change is a later migration.)*
- Round 57 (my feedback): Remove the manual handover block and merge the two file sentences. *(Start was never depending on it — it prepares its own run — so there was one route all along and two on screen.)*
- Round 58 (my feedback): Plan and Execute buttons, validation on both, planning status on the item, a Product box for the description, the mockup instruction in the brief, and a Branch-from dropdown. *("Nothing happens" was a silently-disabled button plus a panel that never listened for the queued job finishing — no error was being swallowed.)*
- Round 59 (my feedback): AI policy belongs in Admin, not Product; also surfaced the per-complexity model/effort tiers (buried two folds deep) and added the missing "What this is" box on Product's board.
- Round 60 (my feedback): Permission at Product level with a per-Solution override, and developer/QA routing defaults — data layer built and tested; the gates are not rewired yet.
- Round 61 (my feedback): AI planning policy doesn't work, and "per work item" is still there. *(The first was a regression I introduced in round 60 — a changed command signature with an un-updated caller, invisible to every gate because the Tauri boundary is not typechecked. The second was fair: I stopped after the data layer and the screen still said the old thing.)*
- Round 62 (my feedback): Rewire the gates. *(Permission now resolves Solution → Product → no, and routing stays the work item's — so what you set in Admin is what is enforced.)*
- Round 63 (my feedback): Developer and QA routing defaults; also two bugs the redesign caused — the Product policy switches would not save (DTO missing the new fields) and the Plan button was disabled because the validation still asked the work item's policy after permission moved to the Product.
- Round 64 (my feedback): Plan and Execute greyed out, and the AI plan needs its own tab where it can be changed. *(The buttons were waiting on a job queued when the app last closed — the startup sweep only cleared "running", and nothing ever reads the queue again. The plan now has an "AI planning" tab: a row per file with its reason, editable by hand or revisable by telling the AI what to change, which sends the instruction with the current plan so it revises rather than starting over. Also added a test that compares every `invoke` call against the command it calls — the seam three of the last four regressions came through — which immediately found the model-price wrapper sending the wrong key.)*
- Round 65 (my feedback): Show the plan in the agent tree, and once planning has succeeded show only Execute. *(The plan is now a block in the Agent briefing under Develop → Work → Ready, a row per file with the AI's reason. Execute stops re-planning what is already planned — it approves what is on screen and starts the agent — and Plan disappears once there is a plan, because re-planning is a decision about the plan you are reading and belongs beside it on the AI planning tab.)*
- Round 66 (my feedback): A git section on the work item — create a repo (public or private) or link one — the same on a Solution clicked in Developer Planning, the code and the work item on separate tabs in Build, and the terminal only when debugging. *("Not a git repository" was a dead end: nothing in the app offered `git init`, and initialising without a first commit would only have produced the next refusal, so it does both. The git panel now leads with the folder and is one component in three places. In Build, picking an agent no longer stacks its workbench on top of the code pane, and the console has become a Debug output tab that does not exist until Debug is pressed.)*
- Round 67 (my feedback): Wire the remote and push, code should appear when a file is clicked, every section should pull out, and find out why Execute would not run. *(Execute planned fine — it stopped at the worktree, because the Solution's folder is not a git repository, so that press now names the Git tab and the button on it. "Hello world (no AI budget is set…)" was a *successful* job with its routing reason in brackets, which read as the reason it failed; only a handover or a budget warning is repeated now. Create new connects `origin` and pushes, saying how far it got. The Debug output tab appears when a session starts and goes when the last one stops. The work item and any open file each open in their own window.)*
- Round 68 (my feedback): Add a get_work_item command and use it in the pull-out; Execute still not working. *(The command exists and the pulled-out window makes one read instead of walking every Product. On Execute: part of "nothing happens" was the panel's error message deleting itself — one error state, cleared by every background reload, and the changes block mounted inside the panel triggers one. Failures from a press now stay on screen until the next press.)*
- Round 69 (my feedback): Execute failed and no error appeared — put an AI error box on the right under Review and ship. *(The message was being said inside the build plan, four components deep and far enough down the panel to have scrolled off. Failures are now reported to one place and shown at the top of the rail — what was pressed, the backend's own words, and a Dismiss that only a person can press.)*
- Round 70 (my feedback): "Execute failed: 'Shop API' has no folder on this machine", and a section showing what the AI could not do with the developer's answer. *(The Git tab can point a Solution at a folder now, beside the sentence that names the problem. Execute also stopped at the first refusal, so one broken repository out of several read as a total failure — it attempts them all and says what started and what refused. The workbench's Questions tab is "AI feedback": failed attempts and what the AI could not do as read-only lists, each with a "how to solve it" the developer writes, which travels into the next attempt.)*
- Round 71 (my feedback): Is it displaying the AI error? *(It was displaying two of the four. Failures thrown by a press reached the box; the AI **declining** did not — that comes back as a result rather than an error, so nothing caught it — and neither did a queued planning job failing, because that is nobody's press. Both reach it now, the job's exactly once.)*
- Round 72 (my feedback): Work items and editing in Production and QA, and steps defined in Rules for each handover — with Product seeing the whole life and Dev and QA only their bits. *(Three fixed gates — Product → Develop, Develop → QA, QA → release — with the steps under each written by you in Develop → Rules. Product's board shows all three on a card; the build plan shows Develop's; QA's new work-items panel shows QA's. Editing the list keeps the ticks already made. QA can move an item along and tick its own checks but not reword the description — that stays Product's, and asking is the way across.)*
- Round 73 (my feedback): Nine identical "no AI policy" failures, is this in Admin, and move Product's and QA's checklists into their own areas. *(Those nine are old rows — that wording was removed in round 62 — shown undated by a panel that listed every attempt, so history read as news. Failures are now grouped by message with a count and a date. Admin holds the policy to change, not the failures. Product's handover checklist is in Product → Strategy and QA's beside the Testing Strategy; Develop keeps its own in Rules.)*
- Round 74 (my feedback): Clear the queue for this item. *(A button on the item's AI feedback panel forgets its settled attempts and says how many went. It never touches a job still queued or running — that row belongs to the runner about to write to it — and never the ledger, so what the calls cost survives being tidied.)*
