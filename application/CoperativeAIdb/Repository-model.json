{
  "form": "database-model",
  "modelName": "Repository",
  "status": "built",
  "dependsOn": {
    "guidance": "Briefs that must be built before this one — usually the tables this model's relationships point at.",
    "example": ["ClothingDatabase/Customer.json"],
    "entries": []
  },
  "instructions": {
    "whoFillsThisIn": "Developers (Product can sanity-check the information being stored).",
    "when": "Whenever a page needs to store data. One of these per model/table.",
    "how": "Answer in plain English where you can.",
    "note": "Solution = the folder this file sits in. Table / object name = this file's name."
  },
  "whatThisModelIs": {
    "question": "What does this model represent, in one line?",
    "example": "A customer's order.",
    "answer": "A code repository the user has registered to work on in the app (multi-repository support)."
  },
  "fields": {
    "guidance": "One entry per piece of data.",
    "entries": [
      { "fieldName": "id", "type": "id / key", "nullable": false, "default": "auto", "rules": "unique", "description": "The unique identifier" },
      { "fieldName": "name", "type": "text", "nullable": false, "default": "", "rules": "unique", "description": "Display name of the repository" },
      { "fieldName": "localPath", "type": "text", "nullable": false, "default": "", "rules": "unique; must be an existing directory when saved", "description": "The repository's local folder path" },
      { "fieldName": "remoteUrl", "type": "text", "nullable": true, "default": "", "rules": "", "description": "The repository's remote URL, if any" },
      { "fieldName": "defaultBranch", "type": "text", "nullable": true, "default": "", "rules": "", "description": "The default branch name, e.g. main" },
      { "fieldName": "isActive", "type": "true/false", "nullable": false, "default": "false", "rules": "at most one repository is active at a time", "description": "Whether this is the currently active repository" },
      { "fieldName": "createdAt", "type": "date", "nullable": false, "default": "now", "rules": "", "description": "When the repository was registered" }
    ]
  },
  "relationships": {
    "guidance": "How this model connects to others. Leave entries empty if it stands alone.",
    "entries": []
  },
  "lookupsAndPerformance": {
    "question": "Which fields do we search or filter on a lot?",
    "answer": ["name", "isActive"]
  },
  "lifecycleAndRules": {
    "retention": {
      "question": "How long do we keep this data / when is it deleted?",
      "answer": "Until the user removes the repository from the app. Removing the entry never deletes the folder on disk."
    },
    "sensitiveFields": {
      "question": "Any sensitive fields that need extra protection?",
      "answer": "No."
    },
    "invariants": {
      "question": "Anything that must always be true?",
      "answer": "At most one repository has isActive = true. localPath is validated (exists, is a directory) before being saved."
    }
  },
  "changesOverTime": {
    "guidance": "Each time you come back to change this model, add an entry describing what should change.",
    "rounds": []
  }
}
