{
  "form": "database-model",
  "modelName": "FeatureDesign",
  "status": "built",
  "dependsOn": {
    "guidance": "Briefs that must be built before this one — usually the tables this model's relationships point at.",
    "example": ["ClothingDatabase/Customer.json"],
    "entries": ["CoperativeAIdb/WorkItem-model.json"]
  },
  "instructions": {
    "whoFillsThisIn": "Developers (Product can sanity-check the information being stored).",
    "when": "Whenever a page needs to store data. One of these per model/table.",
    "how": "Answer in plain English where you can.",
    "note": "Solution = the folder this file sits in. Table / object name = this file's name."
  },
  "whatThisModelIs": {
    "question": "What does this model represent, in one line?",
    "example": "A customer's order.",
    "answer": "A work item's drag-and-drop feature design: the blocks on the canvas (UI pieces, endpoints, data models), their positions, and the connections between them."
  },
  "fields": {
    "guidance": "One entry per piece of data.",
    "entries": [
      { "fieldName": "id", "type": "id / key", "nullable": false, "default": "auto", "rules": "unique", "description": "The unique identifier" },
      { "fieldName": "workItemId", "type": "id / key", "nullable": false, "default": "", "rules": "unique; must reference a WorkItem row", "description": "The work item this design belongs to (one design per item)" },
      { "fieldName": "canvas", "type": "text", "nullable": false, "default": "{}", "rules": "valid JSON", "description": "The design as JSON: blocks (type: ui / endpoint / model; name; description; x; y) and connections (fromBlock, toBlock)" },
      { "fieldName": "createdAt", "type": "date", "nullable": false, "default": "now", "rules": "", "description": "When the design was created" },
      { "fieldName": "updatedAt", "type": "date", "nullable": false, "default": "now", "rules": "updated on every save", "description": "When the design was last saved" }
    ]
  },
  "relationships": {
    "guidance": "How this model connects to others.",
    "entries": [
      { "field": "workItemId", "relationship": "one-to-one", "relatedTable": "WorkItem", "relatedField": "id", "constraintName": "FK_FeatureDesign_WorkItem" }
    ]
  },
  "lookupsAndPerformance": {
    "question": "Which fields do we search or filter on a lot?",
    "answer": ["workItemId"]
  },
  "lifecycleAndRules": {
    "retention": {
      "question": "How long do we keep this data / when is it deleted?",
      "answer": "Deleted with its work item."
    },
    "sensitiveFields": {
      "question": "Any sensitive fields that need extra protection?",
      "answer": "No."
    },
    "invariants": {
      "question": "Anything that must always be true?",
      "answer": "One design per work item at most. The canvas field always parses as valid JSON; connections only reference blocks that exist in the same design."
    }
  },
  "changesOverTime": {
    "guidance": "Each time you come back to change this model, add an entry describing what should change.",
    "rounds": []
  }
}
