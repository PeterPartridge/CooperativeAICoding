{
  "form": "database-model",
  "modelName": "TeamMember",
  "status": "built",
  "dependsOn": {
    "guidance": "Briefs that must be built before this one — usually the tables this model's relationships point at.",
    "example": ["ClothingDatabase/Customer.json"],
    "entries": []
  },
  "instructions": {
    "whoFillsThisIn": "Developers (Product can sanity-check the information being stored).",
    "when": "Whenever a page needs to store data. One of these per model/table.",
    "how": "Answer in plain English where you can.",
    "note": "Solution = the folder this file sits in. Table / object name = this file's name."
  },
  "whatThisModelIs": {
    "question": "What does this model represent, in one line?",
    "example": "A customer's order.",
    "answer": "A team member set up in the Developer Area (Develop tab) — a name and role used to assign work items to people. Not a login: the app has no accounts."
  },
  "fields": {
    "guidance": "One entry per piece of data.",
    "entries": [
      { "fieldName": "id", "type": "id / key", "nullable": false, "default": "auto", "rules": "unique", "description": "The unique identifier" },
      { "fieldName": "name", "type": "text", "nullable": false, "default": "", "rules": "unique; not empty", "description": "The team member's display name" },
      { "fieldName": "role", "type": "list", "nullable": false, "default": "Developer", "rules": "one of: Developer / QA / Product / Designer", "description": "What the person does on the team" },
      { "fieldName": "createdAt", "type": "date", "nullable": false, "default": "now", "rules": "", "description": "When the member was added" }
    ]
  },
  "relationships": {
    "guidance": "How this model connects to others. Leave entries empty if it stands alone.",
    "entries": []
  },
  "lookupsAndPerformance": {
    "question": "Which fields do we search or filter on a lot?",
    "answer": ["name"]
  },
  "lifecycleAndRules": {
    "retention": {
      "question": "How long do we keep this data / when is it deleted?",
      "answer": "Until removed in the Developer Area. Removing a member clears (nulls) the assignee on their work items — never deletes the items."
    },
    "sensitiveFields": {
      "question": "Any sensitive fields that need extra protection?",
      "answer": "Only a display name — no contact details, no credentials."
    },
    "invariants": {
      "question": "Anything that must always be true?",
      "answer": "name is unique and non-empty; role stays in its list; removing a member never removes work."
    }
  },
  "changesOverTime": {
    "guidance": "Each time you come back to change this model, add an entry describing what should change.",
    "rounds": [
      { "round": 2, "change": "A member now carries an assigned Role (roleId FK → Role) instead of a free role-name string; the role gates what the member sees when they are the active user. Managed in the Admin area." }
    ]
  }
}
