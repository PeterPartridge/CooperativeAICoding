{
  "form": "database-model",
  "modelName": "UserCredentials",
  "status": "filled",
  "dependsOn": {
    "guidance": "Briefs that must be built before this one — usually the tables this model's relationships point at.",
    "example": ["ClothingDatabase/Customer.json"],
    "entries": ["Role-model.json"],
    "note": "The First Run Setup page (CoperativeAI/firstRunSetup.md) is the only place that creates a UserCredentials row before any exist — see its brief for the bootstrap flow."
  },
  "instructions": {
    "whoFillsThisIn": "Developers (Product can sanity-check the information being stored).",
    "when": "Whenever a page needs to store data. One of these per model/table. Copy it into the same solution folder as the page it belongs to and link it from that page.",
    "how": "Answer in plain English where you can. Then hand it to Claude using the bridge in claude-only/1-translate-to-claude.md along with the page it serves.",
    "note": "Solution = the folder this file sits in. Table / object name = this file's name. The database engine is set once for the whole solution in that folder's application-spec.json."
  },
  "whatThisModelIs": {
    "question": "What does this model represent, in one line?",
    "example": "A customer's order.",
    "answer": "Credentials for a user"
  },
  "fields": {
    "guidance": "One entry per piece of data. \"type\" can be plain (text, number, date, true/false, money, list) — Developers can map it to the real database type.",
    "entries": [
      { "fieldName": "id", "type": "id / key", "nullable": false, "default": "auto", "rules": "unique", "description": "The unique identifier" },
      { "fieldName": "passwordHash", "type": "string", "nullable": false, "default": "auto", "rules": "", "description": "encrypted string" },
      { "fieldName": "username", "type": "string", "nullable": false, "default": "auto", "rules": "unique", "description": "encrypted string" },
      { "fieldName": "role", "type": "id / key", "nullable": false, "default": "none", "rules": "foreign key -> Role.id", "description": "The Role assigned to this user (exactly one per user)." },
      { "fieldName": "createdAt", "type": "date", "nullable": false, "default": "auto", "rules": "", "description": "When the account was created." },
      { "fieldName": "updatedAt", "type": "date", "nullable": false, "default": "auto", "rules": "", "description": "When the account was last updated." },
      { "fieldName": "lastLogin", "type": "date", "nullable": true, "default": "auto", "rules": "", "description": "When the user last successfully logged in." },
      { "fieldName": "failedLoginAttempts", "type": "number", "nullable": false, "default": "auto", "rules": "", "description": "Count of consecutive failed login attempts, used to trigger a lockout." },
      { "fieldName": "isLocked", "type": "true/false", "nullable": false, "default": "auto", "rules": "", "description": "Whether the account is currently locked out due to failed login attempts." },
      { "fieldName": "isActive", "type": "true/false", "nullable": false, "default": "auto", "rules": "", "description": "Whether the account is active; inactive accounts cannot log in." }

    ]
  },
  "relationships": {
    "guidance": "How this model connects to others. Leave entries empty if it stands alone.",
    "exampleEntry": { "field": "", "relationship": "one-to-many / many-to-one / many-to-many", "relatedTable": "", "relatedField": "", "constraintName": "" },
    "entries": [
      { "field": "role", "relationship": "many-to-one", "relatedTable": "Role", "relatedField": "id", "constraintName": "fk_userCredentials_role" }
    ]
  },
  "lookupsAndPerformance": {
    "question": "Which fields do we search or filter on a lot?",
    "guidance": "These usually need an index so the page stays fast. One entry per field.",
    "answer": ["username", "role"]
  },
  "lifecycleAndRules": {
    "retention": {
      "question": "How long do we keep this data / when is it deleted?",
      "answer": "Kept while the account exists. The very first account (Super Admin) is created by the person themselves via the First Run Setup page when the table is empty — see CoperativeAI/firstRunSetup.md. There is no seeded default password. After that, a Super Admin can create additional users."
    },
    "sensitiveFields": {
      "question": "Any sensitive fields (personal data, payment info) that need extra protection?",
      "answer": "Username, passwordHash"
    },
    "invariants": {
      "question": "Anything that must always be true?",
      "example": "An order must belong to a customer.",
      "answer": "Every user has exactly one role, referencing an existing Role. There must always be at least one active, non-locked user with the Super Admin role — deleting or locking the last one is not allowed."
    }
  },
  "changesOverTime": {
    "guidance": "You don't fill this in at the start. Each time you come back to change this model, add an entry describing what should change. Keep changes small.",
    "exampleEntry": { "round": 2, "change": "Add a lastLogin date field, nullable." },
    "rounds": []
  }
}
