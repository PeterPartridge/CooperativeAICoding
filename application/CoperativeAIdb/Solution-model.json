{
  "form": "database-model",
  "modelName": "Solution",
  "status": "built",
  "dependsOn": {
    "guidance": "Briefs that must be built before this one — usually the tables this model's relationships point at.",
    "example": ["ClothingDatabase/Customer.json"],
    "entries": ["CoperativeAIdb/Product-model.json"]
  },
  "instructions": {
    "whoFillsThisIn": "Developers (Product can sanity-check the information being stored).",
    "when": "Whenever a page needs to store data. One of these per model/table.",
    "how": "Answer in plain English where you can.",
    "note": "Solution = the folder this file sits in. Table / object name = this file's name. Not to be confused with SolutionManagement, which tracks the files of solutions created on disk by the Creation Page — this model is the planning-level Solution a developer creates in the Develop tab and links to a Product."
  },
  "whatThisModelIs": {
    "question": "What does this model represent, in one line?",
    "example": "A customer's order.",
    "answer": "A Solution created in the Develop tab — linked to a Product and filled in from the solution-spec questions (what kind of solution, its purpose, hosting, language, frameworks)."
  },
  "fields": {
    "guidance": "One entry per piece of data.",
    "entries": [
      { "fieldName": "id", "type": "id / key", "nullable": false, "default": "auto", "rules": "unique", "description": "The unique identifier" },
      { "fieldName": "name", "type": "text", "nullable": false, "default": "", "rules": "not empty; unique within its Product", "description": "The Solution's name" },
      { "fieldName": "productId", "type": "id / key", "nullable": false, "default": "", "rules": "must reference a Product row", "description": "The Product this Solution belongs to" },
      { "fieldName": "solutionType", "type": "list", "nullable": false, "default": "application", "rules": "one of: website / api / database / application", "description": "What kind of solution it is (same list as the framework's solution spec)" },
      { "fieldName": "answers", "type": "text", "nullable": false, "default": "{}", "rules": "valid JSON keyed by solution-spec question ids (purpose, hosting, language, frameworks)", "description": "The developer's plain-English solution-spec answers" },
      { "fieldName": "createdAt", "type": "date", "nullable": false, "default": "now", "rules": "", "description": "When the Solution was created" },
      { "fieldName": "updatedAt", "type": "date", "nullable": false, "default": "now", "rules": "updated on every change", "description": "When the Solution last changed" }
    ]
  },
  "relationships": {
    "guidance": "How this model connects to others.",
    "entries": [
      { "field": "productId", "relationship": "many-to-one", "relatedTable": "Product", "relatedField": "id", "constraintName": "FK_Solution_Product" }
    ]
  },
  "lookupsAndPerformance": {
    "question": "Which fields do we search or filter on a lot?",
    "answer": ["productId"]
  },
  "lifecycleAndRules": {
    "retention": {
      "question": "How long do we keep this data / when is it deleted?",
      "answer": "Deleted with its Product, or when the user removes the Solution."
    },
    "sensitiveFields": {
      "question": "Any sensitive fields that need extra protection?",
      "answer": "No."
    },
    "invariants": {
      "question": "Anything that must always be true?",
      "answer": "A Solution always belongs to an existing Product; solutionType stays in its list; answers always parses as JSON."
    }
  },
  "changesOverTime": {
    "guidance": "Each time you come back to change this model, add an entry describing what should change.",
    "rounds": []
  }
}
