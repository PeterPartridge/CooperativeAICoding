{
  "form": "database-model",
  "modelName": "Sprint",
  "status": "built",
  "dependsOn": {
    "guidance": "Briefs that must be built before this one — usually the tables this model's relationships point at.",
    "example": ["ClothingDatabase/Customer.json"],
    "entries": ["CoperativeAIdb/Product-model.json"]
  },
  "instructions": {
    "whoFillsThisIn": "Developers (Product can sanity-check the information being stored).",
    "when": "Whenever a page needs to store data. One of these per model/table.",
    "how": "Answer in plain English where you can.",
    "note": "Solution = the folder this file sits in. Table / object name = this file's name."
  },
  "whatThisModelIs": {
    "question": "What does this model represent, in one line?",
    "example": "A customer's order.",
    "answer": "A sprint belonging to a Product — a named block of time work items can be scheduled into. Dates are optional: teams that don't plan with dates still get named sprints, and teams that don't use sprints at all simply never create one."
  },
  "fields": {
    "guidance": "One entry per piece of data.",
    "entries": [
      { "fieldName": "id", "type": "id / key", "nullable": false, "default": "auto", "rules": "unique", "description": "The unique identifier" },
      { "fieldName": "productId", "type": "id / key", "nullable": false, "default": "", "rules": "must reference a Product row", "description": "The Product this sprint belongs to" },
      { "fieldName": "name", "type": "text", "nullable": false, "default": "", "rules": "not empty; unique within its Product", "description": "The sprint's name, e.g. Sprint 1" },
      { "fieldName": "startDate", "type": "date", "nullable": true, "default": "", "rules": "", "description": "Optional start date" },
      { "fieldName": "endDate", "type": "date", "nullable": true, "default": "", "rules": "must not be before startDate when both are set", "description": "Optional end date" },
      { "fieldName": "createdAt", "type": "date", "nullable": false, "default": "now", "rules": "", "description": "When the sprint was created" }
    ]
  },
  "relationships": {
    "guidance": "How this model connects to others.",
    "entries": [
      { "field": "productId", "relationship": "many-to-one", "relatedTable": "Product", "relatedField": "id", "constraintName": "FK_Sprint_Product" }
    ]
  },
  "lookupsAndPerformance": {
    "question": "Which fields do we search or filter on a lot?",
    "answer": ["productId"]
  },
  "lifecycleAndRules": {
    "retention": {
      "question": "How long do we keep this data / when is it deleted?",
      "answer": "Deleted with its Product, or when the user removes the sprint — removing a sprint clears (nulls) the sprint on its work items, never deletes them."
    },
    "sensitiveFields": {
      "question": "Any sensitive fields that need extra protection?",
      "answer": "No."
    },
    "invariants": {
      "question": "Anything that must always be true?",
      "answer": "A sprint always belongs to an existing Product; when both dates are set, endDate is not before startDate; deleting a sprint never deletes work items."
    }
  },
  "changesOverTime": {
    "guidance": "Each time you come back to change this model, add an entry describing what should change.",
    "rounds": []
  }
}
