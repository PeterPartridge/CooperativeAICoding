{
  "form": "database-model",
  "modelName": "SystemSetting",
  "status": "built",
  "dependsOn": {
    "guidance": "Briefs that must be built before this one — usually the tables this model's relationships point at.",
    "example": ["ClothingDatabase/Customer.json"],
    "entries": []
  },
  "instructions": {
    "whoFillsThisIn": "Developers (Product can sanity-check the information being stored).",
    "when": "Whenever a page needs to store data. One of these per model/table.",
    "how": "Answer in plain English where you can.",
    "note": "Solution = the folder this file sits in. Table / object name = this file's name."
  },
  "whatThisModelIs": {
    "question": "What does this model represent, in one line?",
    "example": "A customer's order.",
    "answer": "A named app-wide setting stored as JSON. First settings: planningHierarchy (how Products are planned — which work-item levels exist and their order) and roadmapMode (sprints or kanban)."
  },
  "fields": {
    "guidance": "One entry per piece of data.",
    "entries": [
      { "fieldName": "id", "type": "id / key", "nullable": false, "default": "auto", "rules": "unique", "description": "The unique identifier" },
      { "fieldName": "key", "type": "text", "nullable": false, "default": "", "rules": "unique; not empty", "description": "The setting's name, e.g. planningHierarchy" },
      { "fieldName": "value", "type": "text", "nullable": false, "default": "null", "rules": "valid JSON", "description": "The setting's value as JSON" },
      { "fieldName": "updatedAt", "type": "date", "nullable": false, "default": "now", "rules": "updated on every change", "description": "When the setting last changed" }
    ]
  },
  "relationships": {
    "guidance": "How this model connects to others. Leave entries empty if it stands alone.",
    "entries": []
  },
  "lookupsAndPerformance": {
    "question": "Which fields do we search or filter on a lot?",
    "answer": ["key"]
  },
  "lifecycleAndRules": {
    "retention": {
      "question": "How long do we keep this data / when is it deleted?",
      "answer": "Settings persist for the life of the installation; unknown keys are never invented by the app."
    },
    "sensitiveFields": {
      "question": "Any sensitive fields that need extra protection?",
      "answer": "No — and never store secrets or API keys here (they belong in the OS credential store, per the solution's security rules)."
    },
    "invariants": {
      "question": "Anything that must always be true?",
      "answer": "planningHierarchy is one of exactly three presets: [epic, feature, userStory, task] (default), [feature, userStory, task], [feature, task]. roadmapMode is 'sprints' (default) or 'kanban'. Values always parse as JSON."
    }
  },
  "changesOverTime": {
    "guidance": "Each time you come back to change this model, add an entry describing what should change.",
    "rounds": []
  }
}
