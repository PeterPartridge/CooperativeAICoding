# Code Map — CooperativeAI Solution

> The AI's running inventory of methods it has created. See [`template/claude-only/3-code-map.template.md`](../../template/claude-only/3-code-map.template.md) for the shape and rules.

---

## Solution — CoperativeAI (application, Tauri backend + React frontend)

**Repo:** https://github.com/PeterPartridge/CooperativeAICoding · **Local path:** `app/CoperativeAI/`

**Pinned versions (recorded at scaffold, 2026-07-16):** tauri 2.11.5, tauri-build 2.6.3, turso 0.6.1 (roaring pinned 0.11.3 for rustc 1.89), tokio 1.52.3, serde 1.0.228, serde_json 1.0.150 · react ^19.2.0, vite ^6.4.1, vitest ^3.2.4, typescript ^5.9.3, @tauri-apps/api ^2.9.0, @tauri-apps/cli ^2.9.6.

### Frontend (React + TypeScript)

| Method | File | What it does (one line) | Uses (files → methods) |
|--------|------|--------------------------|--------------------------|
| `App` | src/App.tsx | Root component; renders the workspace shell | `WorkspaceShell` |
| `WorkspaceShell` | src/pages/WorkspaceShell.tsx | The main window: three-tab menu (Product/Develop/Test), active environment panel, colour settings footer | `TabBar`, theme.ts → `loadTabColors`/`saveTabColors`/`applyTabColors` |
| `TabBar` | src/components/TabBar.tsx | Renders the Product/Develop/Test tabs, each coloured by its CSS variable, active tab marked | theme.ts types |
| `loadTabColors` / `saveTabColors` | src/lib/theme.ts | Persist the user's tab colours in localStorage (decision recorded: localStorage, not the DB — frontend-only concern) | — |
| `applyTabColors` | src/lib/theme.ts | Writes the three tab colours onto `:root` CSS variables | — |
| `ProductPlanning` | src/pages/ProductPlanning.tsx | The Product home: Products as cards, "Add a Product" form (Project_brief Product questions), planning settings row; opening a Product renders its workspace | `ProductWorkspace`, `PlanningMethodSetting`, backend.ts |
| `ProductWorkspace` / `WorkspaceScreen` | src/components/ProductWorkspace.tsx | The Product workspace: all three panels (Planning, RoadMap, Overview) showing at once, each with its own pop-out-to-OS-window button | `PlanningBoard`, `RoadMap`, `ProductOverview`, backend.ts → `openScreenWindow` |
| `ProductOverview` | src/components/ProductOverview.tsx | The Overview panel: the Product's brief answers + where its scaffolded framework files live | backend.ts → `getProductScaffold` |
| `PlanningBoard` / `childTypes` | src/components/PlanningBoard.tsx | The hierarchy board: status columns, type badges, status/assignee/sprint selects, "Add sub-item" (types restricted by the active hierarchy + bug/test), AI-story button on features when the hierarchy includes user stories | backend.ts |
| `RoadMap` | src/components/RoadMap.tsx | Lanes per sprint (dates when set, plus Unscheduled) or per status (kanban) per the roadmapMode setting; sprint-create form; hierarchy items only | backend.ts |
| `PlanningMethodSetting` | src/components/PlanningMethodSetting.tsx | The "How Products are planned" selects: hierarchy preset + roadmap mode | backend.ts settings wrappers |
| `DevelopSolutions` | src/pages/DevelopSolutions.tsx | The Develop environment: Create-a-Solution card + solutions list + AI Settings (team management moved to Admin) | backend.ts, `AiSettings` |
| `AdminArea` | src/pages/AdminArea.tsx | Manage team members + assign roles; roles table with area + field-visibility toggles (Admin role protected) | backend.ts roles/members |
| `PermissionProvider` / `usePermissions` | src/lib/permissions.tsx | Loads the active user's effective permissions (full-access safe default); `canAccess(area)` / `canSeeField(field)` gate tabs and cost fields | backend.ts → `getActivePermissions` |
| `ActiveUserPicker` | src/components/ActiveUserPicker.tsx | "Working as…" header picker; persists the active member and reloads the permission gate | backend.ts, permissions |
| `ProductStrategy` / `PlanningScreen` | src/components/{ProductStrategy,PlanningScreen}.tsx | Product Strategy (structured fields + deliverables + work-by-deliverable + per-deliverable **Generate work** button + the Product AI policy panel), composed above the board in the Planning screen | backend.ts strategy/deliverables, `ProductAiPolicy`, `generateDeliverableWork` |
| `StrategyEditor` | src/components/StrategyEditor.tsx | Generic structured-strategy editor (labelled textareas → one JSON doc per product+area); used by Develop (`develop`) and later Test (`test`) | backend.ts strategy + `DEVELOP_STRATEGY_FIELDS`/`TEST_STRATEGY_FIELDS` |
| `WorkItemViews` | src/components/WorkItemViews.tsx | Developer Board / Sprint / List views over a Product's work items, filterable by assigned user | backend.ts work items/sprints/members |
| `DevelopSolutions` (round 3) | src/pages/DevelopSolutions.tsx | Now: Product picker → Technical Strategy + work views, then Create-a-Solution + GitHub + AI Settings | `StrategyEditor`, `WorkItemViews`, `GithubCard`, `SolutionRepo`, `AiSettings` |
| `TestArea` | src/pages/TestArea.tsx | The Test environment: Product picker → Testing Strategy (`StrategyEditor`, area `test`) + Test Cases; replaces the Test tab placeholder | `StrategyEditor`, `TestCases`, backend.ts |
| `TestCases` | src/components/TestCases.tsx | QA's scenarios: add form + per-case association picker (one control spanning Deliverables and Work Items), state picker, and a test-file field once implemented; optimistic insert | backend.ts test-case wrappers |
| `GithubCard` | src/components/GithubCard.tsx | GitHub connection: token entered once (password field, cleared after storing, verified before it is saved) and held in the OS credential store; connect / disconnect | backend.ts github wrappers |
| `SolutionRepo` | src/components/SolutionRepo.tsx | Per-Solution repository row: shows the linked repo (URL, visibility, created/imported) or offers Link-existing (URL) and Create-new (name + Private); Create-new disabled until GitHub is connected | backend.ts `linkSolutionRepo`/`createSolutionRepo` |
| `FolderField` / `pickFolder` | src/components/FolderField.tsx, src/lib/backend.ts | Native OS folder picker (dialog plugin) reused wherever a folder is chosen | @tauri-apps/plugin-dialog |
| `PopOutHandle` (in ProductWorkspace) | src/components/ProductWorkspace.tsx | Drag handle (HTML5 drag) that tears a panel out into its own OS window — a drag gesture, not a button | backend.ts → `openScreenWindow` |
| `AiSettings` | src/components/AiSettings.tsx | Provider management: add (password-type key field, cleared after storing), list with key stored/not-stored state, test connection, remove; suggests Claude + claude-opus-4-8 defaults | backend.ts AI wrappers |
| `ProductAiPolicy` | src/components/ProductAiPolicy.tsx | Product-level AI policy panel (deny-by-default): allow read / allow generate / provider / effort — what gates "Generate work" on a Deliverable; states in the panel that it covers every Deliverable | backend.ts product-policy wrappers |
| `PolicyEditor` | src/components/PolicyEditor.tsx | Per-card AI policy panel: allow read/edit/generate-tests switches (deny-by-default), provider picker, effort tier | backend.ts policy wrappers |
| `StandaloneScreen` | src/pages/StandaloneScreen.tsx | What a pulled-out OS window renders: one workspace screen for one Product with its title | `WorkspaceScreen`, backend.ts → `getProduct` |
| standalone routing | src/main.tsx | `?window=<screen>&productId=<id>` renders `StandaloneScreen` instead of the shell | `StandaloneScreen` |
| backend.ts wrappers | src/lib/backend.ts | One mockable module wrapping every Tauri `invoke` call (products, solutions + github, team members, roles, deliverables, strategy, sprints, settings, work items incl. `generateUserStories`, `openScreenWindow`, repositories) + shared types, HIERARCHY_PRESETS/ROADMAP_MODES/PRODUCT_QUESTIONS/SOLUTION_QUESTIONS/DEVELOP_STRATEGY_FIELDS/TEST_STRATEGY_FIELDS/DEV_VIEWS consts | @tauri-apps/api → `invoke` |

### Backend (Rust, src-tauri)

| Method | File | What it does (one line) | Uses (files → methods) |
|--------|------|--------------------------|--------------------------|
| `main` | src-tauri/src/main.rs | App entry; resolves the data dir (`COPERATIVEAI_DATA_DIR` or OS app-data), opens CoperativeAIdb, creates all tables, manages the shared connection, registers all commands | `db::connect`, `db::create_all_tables`, `commands::AppDb` |
| `commands::AppDb` | src-tauri/src/commands/mod.rs | Managed state: the app's shared db connection behind a tokio Mutex; `to_message` maps DbError to a frontend string | — |
| `work_items::{list_work_items, create_work_item, update_work_item_status, update_work_item, delete_work_item}` | src-tauri/src/commands/work_items.rs | Planning board commands (per-product listing; create with parent; assign/schedule/risk/Solution) + `WorkItemDto` | `db::work_item`, `AppDb` |
| `work_items::{list_work_item_links, link_work_items, unlink_work_items}` | src-tauri/src/commands/work_items.rs | Dependency links. The list is **scoped to a Product, not an item**, so a board of fifty cards costs one round trip rather than fifty | `db::work_item_link` |
| `work_items::generate_user_stories` | src-tauri/src/commands/work_items.rs | The AI-story hook: feature-type + hierarchy-includes-userStories + deny-by-default policy gates, all tested; real generation ships with the AI-integration build | `db::work_item`, `db::system_setting`, `db::work_item_policy` |
| `products::{list_products, create_product, get_product, get_product_scaffold, delete_product}` | src-tauri/src/commands/products.rs | Product home commands + `ProductDto`; create_product scaffolds framework files when a folder is given (rollback on failure, registered in SolutionManagement); get_product_scaffold feeds the Overview panel | `db::product`, `db::solution_management`, `scaffold`, `AppDb` |
| `emit::{solution_spec, page_brief, safe_stem, write_files, content_hash}` | src-tauri/src/emit.rs | Turns app data into the framework's own files (solution specs, page briefs). Content builders are pure; `write_files` **never overwrites a hand edit** — a file changed since the app wrote it, or never written by it, is reported as a conflict and left alone. FNV-1a hash (stable across toolchains, unlike `DefaultHasher`) | `db::emitted_file` |
| `emitted_file::{record, list_for_product}` | src-tauri/src/db/emitted_file.rs | The hash of each file **as the app wrote it** — the record that makes conflict detection possible. Its own table so `solution_management`'s scaffold rows survive | — |
| `commands::emit::generate_framework_files` | src-tauri/src/commands/emit.rs | Builds the file set from Solutions and planned features, writes under the Product's scaffold root, records hashes, returns written/unchanged/conflicts | `emit`, `db::emitted_file` |
| `FrameworkFiles` | src/components/FrameworkFiles.tsx | Develop-area action: generates the files and names any it left alone, stating the edits are safe | backend.ts `generateFrameworkFiles` |
| `scaffold::scaffold_product` | src-tauri/src/scaffold.rs | Generates `<folder>/<name>/.CoperativeAI/` — Project_brief.md prefilled from the Product's answers (Part 1 + Part 3), claude-only/, README — with validated parent and filesystem-safe naming | serde_json, std::fs |
| `solutions::{list_solutions, create_solution, delete_solution}` | src-tauri/src/commands/solutions.rs | Solution Creation commands + `SolutionDto` (incl. origin / githubUrl / githubVisibility) | `db::solution`, `AppDb` |
| `test_cases::{list_test_cases, create_test_case, update_test_case, delete_test_case}` | src-tauri/src/commands/test_cases.rs | Test-case commands + `TestCaseDto` | `db::test_case`, `AppDb` |
| `github::{github_status, set_github_token, remove_github_token, link_solution_repo, create_solution_repo}` | src-tauri/src/commands/github.rs | GitHub commands: local token status, verify-then-store the token, link an existing repo by URL, or create one (private/public) and link it | `crate::github`, `db::solution::set_github`, `AppDb` |
| `team_members::{list_team_members, add_team_member, remove_team_member}` | src-tauri/src/commands/team_members.rs | Developer Area commands + `TeamMemberDto` | `db::team_member`, `AppDb` |
| `sprints::{list_sprints, create_sprint, remove_sprint}` | src-tauri/src/commands/sprints.rs | RoadMap sprint commands + `SprintDto` | `db::sprint`, `AppDb` |
| `settings::{get/set_planning_hierarchy, get/set_roadmap_mode}` | src-tauri/src/commands/settings.rs | The "How Products are planned" settings commands | `db::system_setting`, `AppDb` |
| `windows::open_screen_window` | src-tauri/src/commands/windows.rs | Pull-out windows: opens (or focuses) `index.html?window=<screen>&productId=<id>` as its own OS window via WebviewWindowBuilder — Rust-side, no JS window capabilities | tauri → `WebviewWindowBuilder`, `Manager` |
| `ai::client::{Generated, parse_generation}` | src-tauri/src/ai/client.rs | The escape hatch: a response is either `Items` or `Blocked{reason, whatIsNeeded}`. A hedged response (both filled) reads as **blocked** — a question costs less than work built on a misunderstanding | serde_json |
| `model_install::{sync_for_provider, is_installed, set_result}` | src-tauri/src/db/model_install.rs | Which models the platform has seen and whether each passed installation. Detection is a diff against what a provider lists; **anything not `installed` is refused** | `ai_provider` |
| `pack::{build, system_instructions, developer_rules_doc, architecture_templates}` | src-tauri/src/pack.rs | The Model Capability Pack — the framework's rules as a token-efficient system prompt plus the schemas the platform parses. Emitted as files so it can be read and diffed; a size test guards the prompt because every call pays for it | `db::developer_rules`, `emit` |
| `ai::validation::{check_work_items, check_strategy, check_declines_vague, ValidationReport}` | src-tauri/src/ai/validation.rs | Structural probes, not similarity-to-Claude: schema conformance, architecture kinds the app can file, no forbidden technology declared, escape hatch used on a hopeless brief. All-or-nothing | `developer_rules::violations` |
| `commands::models::{list_model_status, refresh_provider_models, install_model}` | src-tauri/src/commands/models.rs | Detect on read, re-read a local server, and the install workflow: build pack → write it → run probes → record pass or fail | `pack`, `ai::validation`, `emit::write_generated` |
| `emit::write_generated` | src-tauri/src/emit.rs | Writes files the app **owns and regenerates**, overwriting unconditionally — deliberately separate from `write_files`, which protects authored briefs. A stale pack is worse than a lost edit | — |
| `ModelInstalls` | src/components/ModelInstalls.tsx | Models with their install state, probe-by-probe results, suggested fixes, and Install / Re-validate | backend.ts model wrappers |
| `ai::estimator::{estimate, median_tokens, size_factor}` | src-tauri/src/ai/estimator.rs | What a call will cost and take. Per-purpose baseline scaled by item size, or the **median of ≥20 recorded `ok` calls** once there are enough — the `source` field says which, because a guess shown as confidently as a measurement is dishonest | `model_price::cost_micropence` |
| `commands::recommendations::recommend_for_work_item` | src-tauri/src/commands/recommendations.rs | The fastest and cheapest ways to do a piece of work, drawn from the budget's provider chain so the options match what the router would allow. **Computed on demand, not stored** — prices, budget and history all move | `estimator`, `ai_usage`, `product_budget` |
| `CostRecommendation` | src/components/CostRecommendation.tsx | Both options with tokens/money/minutes, each labelled with where the number came from; unaffordable options marked | backend.ts `recommendForWorkItem` |
| `developer_rules::{set_rules, get_for_product, violations, unlisted}` | src-tauri/src/db/developer_rules.rs | The constraints the AI must obey. `violations` scans the AI's **declared technologies** (never prose — a stack saying "No Java anywhere" was being flagged for obeying) for forbidden technology, **whole-word**, so "Go" does not fire on "Google" while ".NET"/"C++"/"C#" still match. `unlisted` is the softer sibling: not on the allow list, so a question for a person rather than a rule broken. An empty allow list permits everything | `product` |
| `handover::{brief, brief_path, suggested_command}` | src-tauri/src/handover.rs | Assembles everything known about a work item into one brief for a coding agent — description, prior answers, rules, chosen architecture, diagrams, risk, and what waits on it. Ordered job → constraints → context. **Nothing is spawned and no cost is reported**: Claude Code bills separately, so a figure here would be one the app cannot see | `pack::developer_rules_doc` |
| `change_run::{prepare, record_review, settle, list_for_item}` | src-tauri/src/db/change_run.rs | One handover of a work item to an agent. **No cost column, deliberately** — it would hold a guess or a zero and both would be read as fact. `settle` records what the developer says was kept, because the app cannot see what was committed | `work_item`, `solution` |
| `workspace::{resolve_within, read_tree, read_file, read_changes}` | src-tauri/src/workspace.rs | Reading a Solution's working copy. **`resolve_within` is the security boundary** — relative paths from the frontend are untrusted, so root and target are canonicalised (resolving `..` *and* symlinks) and the target must still start with the root. Tree skips generated dirs, caps depth/entries, says when truncated. Changes come from git, including untracked files | `std::fs`, `git` |
| `review::review` | src-tauri/src/review.rs | A diff checked against the Developer Rules. **Checks what a change adds, never what it removes** — a diff deleting the last jQuery mentions jQuery on every removed line, and flagging that reports the fix as the fault. Missing tests are a **notice**, judged across the whole change; test detection reads whole path segments so `src/latest/` is not a test | `developer_rules::violations`, `workspace::FileChange` |
| `commands::workspace::*` | src-tauri/src/commands/workspace.rs | Set a Solution's working copy, read its tree/files, and review its changes. `ChangeReviewDto.noRules` distinguishes "nothing was checked" from "nothing was wrong" | `workspace`, `review`, `db::developer_rules` |
| `DiagramView` + `jsonGraphToMermaid` | src/components/DiagramView.tsx | Draws a stored diagram. Mermaid renders Mermaid (the notation was already chosen, so any other renderer would disagree with it); `jsonGraph` converts to a flowchart. **PlantUML is deliberately not drawn** — the browser route posts it to plantuml.com, and a private architecture is not worth sending away for a picture. Loaded on demand (startup bundle +3 kB), rendered `securityLevel: "strict"` so a stored diagram cannot reconfigure the app | `mermaid` (lazy) |
| `diagram::check` | src-tauri/src/diagram.rs | Structural checks for mermaid/plantuml/jsonGraph — **not parsers**, and the module says so. Catches what actually goes wrong: prose where a diagram was asked for, PlantUML that opens and never closes (a truncated response), a JSON-graph edge pointing at a node that does not exist. Shared by design assets and architecture docs so there is one answer to "is this a diagram" | `serde_json` |
| `architecture_doc::{save, list_by_product, find_by_id, unlink_solution, delete}` | src-tauri/src/db/architecture_doc.rs | System interaction / component map / API contract / event flow / infrastructure. **Validated on the way in** — a diagram that does not render is worse than none, because it looks like documentation. `solutionId` nullable: null means the whole Product. Deleting a Solution unlinks its docs rather than deleting them | `diagram::check`, `product`, `solution` |
| `repo_link::{link, unlink, list_for_product, reaches, remove_for_solution}` | src-tauri/src/db/repo_link.rs | How a Product's Solutions depend on each other. **Only `buildsOn` is cycle-checked** — it is the only kind that orders anything; mutual API calls are a real arrangement and refusing them would make the map lie. `reaches` follows *every* kind to any depth (that is how a change propagates) and terminates on a legal runtime cycle | `solution::find_by_id` |
| `commands::architecture::*` | src-tauri/src/commands/architecture.rs | Architecture docs, Solution links, `solutions_reached_by`, and `generate_architecture_doc` — gated by the Product AI policy, routed, ledgered, dispatched by provider kind | `db::architecture_doc`, `db::repo_link`, `ai_run` |
| `client::{build_architecture_prompt, parse_diagram, GeneratedDiagram}` | src-tauri/src/ai/client.rs | Architecture generation. Existing documents and recorded links travel in the **cacheable half**, and a new diagram is told to agree with them. The parser takes the **caller's** declared format, not the model's — `diagram::check` does the judging, and a test pins that division | `post_structured` |
| `design_asset::{save, record_figma_location, list_by_product, find_by_id, delete}` | src-tauri/src/db/design_asset.rs | What design produced: tokens/flows/diagrams/wireframes/brand. **The kind decides the format, not the caller** — tokens must parse as JSON, flows must start with a Mermaid diagram type, because every consumer assumes it. Regenerating a named asset replaces it | `product::find_by_id` |
| `figma::{store_token, verify, read_file, digest_file, push_variables, post_comment, file_key_from}` | src-tauri/src/figma.rs | Token in the OS credential store. **Figma REST cannot create frames on any plan** (needs a plugin) and **Variables are Enterprise-only**; a 403 says so and names `design/tokens.json` as the way through. `digest_file` reduces a megabyte document to pages/screens/components/copy before it reaches a prompt — capped and marked when partial | `keyring`, `reqwest` |
| `emit::design_files` | src-tauri/src/emit.rs | Design assets → `design/tokens.json`, `ui-flows.md`, `components.md`, `brand.md`. Below Enterprise this is the **only** route tokens have into Figma, so a test pins the emitted path to the path the 403 message promises | — |
| `commands::design::*` | src-tauri/src/commands/design.rs | Assets, the Figma link, file emission, and `generate_design_strategy` — gated by the Product AI policy, routed, ledgered and dispatched by provider kind like every other AI action | `db::design_asset`, `figma`, `ai_run` |
| `client::{build_design_prompt, parse_design, GeneratedDesign}` | src-tauri/src/ai/client.rs | Marketing and design generation. The Figma digest goes in the **cacheable half** — most expensive, least likely to change. Copy from the file is quoted and labelled as content, never instruction. Fences are stripped so a valid diagram is not rejected for wearing one | `post_structured` |
| `sprint_capacity::{set_capacity, load_for_sprint}` | src-tauri/src/db/sprint_capacity.rs | One number per member per sprint, in the team's own unit. `load_for_sprint` returns it beside an **item count, not effort** — work items carry no estimate, and inventing one would be a guess dressed as arithmetic. Someone carrying work with no declared capacity still appears, at zero | `sprint`, `team_member`, `work_item` |
| `work_item_link::{link, unlink, list_for_item, list_for_product, remove_for_item}` | src-tauri/src/db/work_item_link.rs | Dependencies between work items. `blocks` orders work and is cycle-checked by a breadth-first walk; `relatesTo` implies no order so loops of it are allowed. **Cross-repo is derived** from the two items' Solutions, never stored | `work_item::find_by_id` |
| `solution_strategy::{set_strategy, choose_option, get_for_item}` | src-tauri/src/db/solution_strategy.rs | Per work item: strategy, architecture options (JSON), the developer's chosen option (a column), tech stack. Regenerating clears a stale choice | `work_item` |
| `client::{build_solution_strategy_prompt, parse_solution_strategy, generate_solution_strategy}` | src-tauri/src/ai/client.rs | Prompts with disallowed tech as "MUST NOT use", parses strategy + options or the escape hatch | `post_structured` |
| `commands::work_items::resolve_item_ai_gate` | src-tauri/src/commands/work_items.rs | The shared deny-by-default policy gate for any AI action anchored on a work item — extracted so new features cannot skip it | `work_item_policy` |
| `DeveloperRulesEditor` / `SolutionStrategyPanel` | src/components/ | The rules editor, and the per-item "how to build" panel with architecture options, a chooser, and a red banner naming any rule the AI broke | backend.ts |
| `ai_feedback::{record, list_open_for_item, resolve, clarifications_for_item}` | src-tauri/src/db/ai_feedback.rs | Questions the AI raised instead of guessing. `resolve` refuses an empty note; answers become clarifications carried into the next prompt for that item | `work_item` |
| `AiQuestions` | src/components/AiQuestions.tsx | Open AI questions on a work-item card with an answer box; renders nothing when there are none | backend.ts `listAiFeedback`/`resolveAiFeedback` |
| `ai::router::{route, used_pct}` | src-tauri/src/ai/router.rs | **The spend gate.** Pure: budget + ledger spend + providers → use this provider/model, or blocked. Warn → run on; handover → next in chain; hard stop → only an unmetered provider, else refuse before content moves | `ai::tiering` |
| `ai::ollama::{generate_stories, list_models, chat_body}` | src-tauri/src/ai/ollama.rs | Local-model client: `/api/chat` with a JSON schema in `format`, counts from `prompt_eval_count`/`eval_count`, `/api/tags` to list installed models. No key, no cost | reqwest |
| `ai::backend::generate_stories` | src-tauri/src/ai/backend.rs | Enum dispatch over provider kind so call sites never branch on Anthropic vs Ollama; fetches the key only for the metered path | `ai::client`, `ai::ollama`, `ai::keys` |
| `commands::ai_run::{plan, record}` | src-tauri/src/commands/ai_run.rs | The shared beats of a governed AI call: plan under the lock (routing + refusal recorded), call without it, record priced usage after | `ai::router`, `db::ai_usage`, `db::model_price` |
| `ai::tiering::model_for_effort` | src-tauri/src/ai/tiering.rs | Picks the model from the provider's **cheapest-first** list by effort tier (low→first, medium→middle, high→last; unknown→cheapest). Pure, table-tested. Replaced `models.first()` at both AI call sites | — |
| `ai::client::{Prompt, Usage, product_context}` | src-tauri/src/ai/client.rs | Prompts split into a cacheable `context` (Product brief/strategy/solutions, sent first with `cache_control: ephemeral`) and a per-call `task`; `Usage` reads the response's token counts incl. cache reads | reqwest, serde |
| `ai::keys::{store_key, get_key, key_stored, delete_key}` | src-tauri/src/ai/keys.rs | API keys in the OS credential store (keyring 3, service "CoperativeAI") — values never touch the DB/config/logs | keyring crate |
| `github::{store_token, get_token, token_stored, delete_token, verify, create_repo, repo_create_body}` | src-tauri/src/github.rs | GitHub PAT in the OS credential store (same rule as AI keys) + the two REST calls — GET /user to verify, POST /user/repos to create. `repo_create_body` is pure so the request shape is unit-tested without the network | keyring, reqwest |
| `ai::client::{build_story_prompt, parse_stories, generate_stories, test_connection}` | src-tauri/src/ai/client.rs | Claude Messages API over raw HTTPS; structured outputs (json_schema) for guaranteed-parseable stories; refusal stop_reason handled; policy effort tier → output_config.effort | reqwest, serde_json |
| `ai_settings::{list_ai_providers, add_ai_provider, remove_ai_provider, test_ai_provider}` | src-tauri/src/commands/ai_settings.rs | Provider CRUD with keyring-backed keys (add rolls back the row if key storage fails; remove deletes the credential entry) + keyStored flag | `db::ai_provider`, `ai::keys`, `ai::client` |
| `policies::{get_work_item_policy, set_work_item_policy, get_product_policy, set_product_policy}` | src-tauri/src/commands/policies.rs | The policy editors' commands over the two deny-by-default tables (per item, per Product) | `db::work_item_policy`, `db::product_policy` |
| `work_items::{resolve_deliverable_generation, generate_deliverable_work, level_for_deliverable}` | src-tauri/src/commands/work_items.rs | "Generate work" for a Deliverable: gate half (Product policy + hierarchy) split from the network call; `level_for_deliverable` picks the level above user stories (pure, table-tested) | `db::product_policy`, `ai::client::build_deliverable_prompt` |
| `roles::{list/create/update/delete_role, get/set_active_member, get_active_permissions}` | src-tauri/src/commands/roles.rs | Admin role commands + the active-user permission gate (full-access default when no active user) | `db::role`, `db::team_member`, `db::system_setting` |
| `deliverables::{list/create/delete}` + `strategy::{get/save}` | src-tauri/src/commands/{deliverables,strategy}.rs | Deliverable CRUD and per-area strategy get/save | `db::deliverable`, `db::strategy` |
| `products::{…, get_product_scaffold}` / `scaffold::scaffold_product` | src-tauri/src/commands/products.rs, src/scaffold.rs | (from the earlier round) Product scaffolding — see below |  |
| `work_items::{generate_user_stories, resolve_story_generation}` | src-tauri/src/commands/work_items.rs | The AI-story feature: gates (feature type, hierarchy, deny-by-default policy, provider) resolved separately (unit-tested), then key → Claude call with product+solutions context → userStory children created under the feature | `ai::client`, `ai::keys`, `db::product`, `db::solution`, `db::work_item` |
| `repositories::{list_repositories, add_repository}` | src-tauri/src/commands/repositories.rs | Repository commands (Develop-side; full management is its own roadmap item) + `RepositoryDto` | `db::repository`, `AppDb` |
| `db::connect` | src-tauri/src/db/mod.rs | Opens a turso database at a path (or `:memory:` for tests) and returns a connection | turso → `Builder::new_local` |
| `db::create_all_tables` | src-tauri/src/db/mod.rs | Creates every table (solution_management, repositories, work_items, ai_providers, work_item_policies, feature_designs) | each module's `create_table` |
| `db::now_millis` | src-tauri/src/db/mod.rs | Unix-millisecond timestamps for created/updated columns | — |
| `DbError` / `db::Result` | src-tauri/src/db/mod.rs | Error type: `Db` (engine) or `Validation` (code-enforced invariants — the engine doesn't enforce FKs) | — |
| `solution_management::{create_table, create, list_all, delete, last_insert_id}` | src-tauri/src/db/solution_management.rs | SolutionManagement table (PascalCase columns per its brief): create/list/delete solutions | `db::now_millis` |
| `product::{create_table, create, list_all, find_by_id, delete}` | src-tauri/src/db/product.rs | Product table (name + brief answers JSON); delete cascades work items (with their policies/designs), sprints, and solutions | `work_item::delete`, `db::now_millis` |
| `test_case::{create_table, create, list_by_product, find_by_id, update_case, delete}` | src-tauri/src/db/test_case.rs | Test-case table: QA scenarios per Product, state designed/implemented + testPath, associated with a Deliverable and/or a Work Item; `check_links` validates both associations for create and update | `product`/`deliverable`/`work_item` find_by_id |
| `solution::{create_table, create, list_all, list_by_product, find_by_id, set_github, delete}` | src-tauri/src/db/solution.rs | Solution table: linked to a Product, typed, answers JSON; name unique per Product; round 2 adds origin (created/imported) + githubUrl + githubVisibility | `product::find_by_id`, `db::now_millis` |
| `team_member::{create_table, add, list_all, find_by_id, remove}` | src-tauri/src/db/team_member.rs | TeamMember table (name + role, no accounts); remove unassigns items, never deletes them | `db::now_millis` |
| `sprint::{create_table, create, list_by_product, find_by_id, remove}` | src-tauri/src/db/sprint.rs | Sprint table per Product, dates optional (end ≥ start when both); remove unschedules items | `product::find_by_id`, `db::now_millis` |
| `system_setting::{create_table, get, set, get/set_planning_hierarchy, get/set_roadmap_mode}` | src-tauri/src/db/system_setting.rs | Key/JSON settings; planningHierarchy restricted to 3 presets (HIERARCHY_PRESETS), roadmapMode sprints/kanban; defaults when unset; also stores `activeTeamMemberId` | `db::now_millis` |
| `role::{create_table(+seed), list_all, find_by_id, find_by_name, create, update, delete}` | src-tauri/src/db/role.rs | Permission roles (area + field-visibility flags), 4 seeded on first run; Admin never deletable/weakened; role-in-use not deletable | `db::now_millis` |
| `deliverable::{create_table, create, list_by_product, find_by_id, set_dependency, delete}` | src-tauri/src/db/deliverable.rs | Per-Product strategy deliverables; delete unlinks work items. `set_dependency` refuses a self-reference or a transitive cycle (`depends_on_transitively`, 1000-step guard so a corrupted chain errors rather than hangs) | `product::find_by_id` |
| `strategy::{create_table, get, save}` | src-tauri/src/db/strategy.rs | One structured strategy doc per (product, area: product/develop/test), JSON-validated | `product::find_by_id` |
| `repository::{create_table, add, list_all, set_active, find_active, find_by_id, remove}` | src-tauri/src/db/repository.rs | Repository table: register (path must exist), single-active invariant, remove never touches disk | `db::now_millis`, `solution_management::last_insert_id` |
| `work_item::{create_table, create, update_status, update_item, list_by_product, find_by_id, delete}` | src-tauri/src/db/work_item.rs | WorkItem table: belongs to a Product; epic/feature/userStory/task hierarchy invariant vs the active setting (bug/test anywhere); assignee/sprint/date/Solution validation. Round 8 adds `risk` (free text) and `solutionId` via **ALTER TABLE, not the drop-and-recreate used elsewhere** — work items are the team's actual plan, the one thing here nobody could reconstruct | `product::find_by_id`, `team_member::find_by_id`, `sprint::find_by_id`, `solution::find_by_id`, `system_setting::get_planning_hierarchy` |
| `ai_provider::{create_table, add, list_all, find_by_id, remove}` | src-tauri/src/db/ai_provider.rs | AIProvider table: https-only URL, **keyAlias only — key values never enter the DB**; remove nulls referencing policies | `db::now_millis` |
| `work_item_policy::{create_table, set_policy, get_for_item, is_allowed}` | src-tauri/src/db/work_item_policy.rs | Per-item AI policy; `is_allowed` is the deny-by-default gate every AI call must pass (no row/flag/provider mismatch → denied) | `work_item::find_by_id`, `ai_provider::find_by_id` |
| `ai_usage::{record, spend_for_product, list_for_product}` | src-tauri/src/db/ai_usage.rs | The ledger: one row per AI call incl. blocked ones. **Money in micropence** (tokens × pence-per-million lands exactly, no rounding); blocked calls are recorded but excluded from spend | — |
| `model_price::{set_price, find, cost_micropence}` | src-tauri/src/db/model_price.rs | Editable price/throughput per (provider, model); `cost_micropence` prices a call exactly, charging cache reads at ÷10 and writes at ×1.25 | `ai_provider::find_by_id` |
| `product_budget::{set_budget, get_for_product, current_period_start}` | src-tauri/src/db/product_budget.rs | Per-Product budgets, ascending warn/handover/hard-stop thresholds, rolling period, ordered provider chain. Editing amounts keeps the period start | `product`, `ai_provider` |
| `product_policy::{create_table, set_policy, get_for_product}` | src-tauri/src/db/product_policy.rs | Per-**Product** AI policy (allowRead / allowGenerate / provider / effort), deny-by-default; gates Deliverable planning. Coarser than the item policy on purpose — one switch covers every Deliverable | `product::find_by_id`, `ai_provider::find_by_id` |
| `feature_design::{create_table, save, get_for_item}` | src-tauri/src/db/feature_design.rs | One JSON canvas per work item; canvas validated (JSON + connections reference existing blocks) on save | `work_item::find_by_id`, serde_json |

**Test suites:** 355 cargo tests (+5 ignored live checks) (db modules incl. role seeding, deliverable/strategy + dependency cycles, work_item commercial fields + risk/Solution + the data-preserving migration, work_item_link blocking cycles vs allowed relatesTo rings, sprint_capacity, solution GitHub fields + migrations + unlink-work-on-delete, test_case associations, product_policy + deliverable-generation gates + `level_for_deliverable`, model tiering, prompt context/task split invariants, usage parsing, github request bodies, AI gates, scaffold) + 142 Vitest tests (shell, Product home, HandoverPanel incl. the "cannot see what the run costs" statement, DiagramView incl. strict rendering + render-failure fallback + PlantUML refusal + JSON-graph conversion, Solution box incl. path containment messaging + truncated tree + violations vs notices + "no rules" vs "no problems", Marketing & Design incl. Figma connect/read/push refusal + digest cost + file export, Developer Planning incl. cross-repo map + impact walk + refused build cycle + unrenderable diagram, PlanningBoard incl. cost/deliverable/risk/Solution + dependency links + cross-repo labelling + optimistic create, ProductStrategy generate-work + AI policy panel, RoadMap + sprint capacity, DevelopSolutions incl. technical strategy + views + GitHub connect/link/create, WorkItemViews board/sprint/list + user filter, TestArea testing strategy + test cases, AiSettings, AdminArea, permission gating, drag pop-out) — all green as of this build, with `npm run build` and a full `cargo build` clean.

**Live verification.** The Ollama path is **proven** as of 2026-07-18 against a local `ornith:9b`: structured output via `format`, the shared parser, token capture, the **escape hatch** (the model declined a hopeless brief in 91 output tokens against 126 to produce work), and solution-strategy generation with the developer-rule check.

Live testing found two things unit tests could not: strategy generation **did not dispatch on provider kind**, so a budget handover mid-design failed outright; and the rule check **fired on obedience**, flagging a tech stack that said "No Java or PHP anywhere". Both fixed — see `CoperativeAI/developerArea.md` round 7. Re-run with:

```text
OLLAMA_MODEL=ornith:9b cargo test -- --ignored ollama_is_live    --nocapture
OLLAMA_MODEL=ornith:9b cargo test -- --ignored escape_hatch      --nocapture
OLLAMA_MODEL=ornith:9b cargo test -- --ignored strategy_is_live  --nocapture
OLLAMA_MODEL=ornith:9b cargo test -- --ignored install_probes    --nocapture
```

**Still unverified:** the **Claude path** — prompt caching, `usage` capture from the Messages API, and pricing against a real response — plus whether Claude and the *strategy* prompt behave as the story prompt did. Needs a key:

```text
ANTHROPIC_API_KEY=sk-... cargo test -- --ignored caching_is_live
```

**Pinned additions (2026-07-17):** tauri-plugin-dialog 2 (+ `@tauri-apps/plugin-dialog`), a `capabilities/default.json` granting `core:default` + `dialog:default`.

**Pinned additions (2026-07-16):** keyring 3.6.3 (windows-native + sync-secret-service), reqwest 0.12.28 (json).

**Not yet built:** the Feature Designer canvas, spec generation, full repository management, code editor, terminal (ConPTY spike first), and QA test designer — see `application/claude-only/` for translated specs and the roadmap in the plan.

**Standing technical debt:** turso 0.6.x is pre-1.0 (all access isolated in `src-tauri/src/db/` so a swap to libsql/rusqlite is one module); `roaring` pinned to 0.11.3 until rustc ≥1.90; db layer carries a module-level `#[allow(dead_code)]` until commands consume it.

---

## Solution — CoperativeAIdb (turso, embedded in CoperativeAI)

No separate code — CoperativeAIdb ships embedded inside the CoperativeAI binary. Its methods are listed under the CoperativeAI section above (the `src-tauri/src/db/` modules), since they live in the same crate.
