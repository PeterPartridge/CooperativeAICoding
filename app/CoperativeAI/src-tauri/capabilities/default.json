{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core defaults plus the native folder/file picker.",
  "windows": ["*"],
  "permissions": ["core:default", "dialog:default"]
}
