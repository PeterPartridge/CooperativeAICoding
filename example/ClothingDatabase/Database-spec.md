# Database Solution Spec — ClothingDatabase

> **Example** — a filled-in copy of [`_forms/Database-spec.md`](../../_forms/Database-spec.md). The solution is the folder this file sits in (`ClothingDatabase`).

---

**Which database type does it use?**
SQL Server.

**Where will it be deployed / hosted?**
Azure Elastic Pool.

**What can this solution NOT do / not deploy?**
SQL Agent Jobs (not supported on an Elastic Pool).
