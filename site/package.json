{
  "name": "cooperativeaicoding-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Generates the GitHub Pages site. Kept apart from the app so the site cannot drag a dependency into what ships.",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "marked": "^17.0.2"
  }
}
